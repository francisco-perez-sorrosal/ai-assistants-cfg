# Technical Debt Ledger

<!-- Living, append-only ledger of grounded debt findings.
     Producers: verifier (per-change Phase 5/5.5), sentinel (repo-wide TD dimension).
     Consumers: systems-architect, implementation-planner, implementer, test-engineer, doc-engineer.
     Schema, owner-role heuristic, worktree-merge dedupe semantics, and lifecycle conventions
     are defined canonically in skills/software-planning/references/tech-debt-ledger.md
     (rules/swe/agent-intermediate-documents.md retains a 5-line summary + pointer).
     Do not duplicate the schema here — the skill reference is the single source of truth. -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § `TECH_DEBT_LEDGER.md` for field definitions, enum values, the owner-role heuristic, and the post-merge dedupe contract.

**Append new rows at the end of this active ledger.** Consumers update `status`, `resolved-by`, and `last-seen` in place; when status transitions to `resolved` or `wontfix`, the row migrates to the sibling [`TECH_DEBT_RESOLVED.md`](TECH_DEBT_RESOLVED.md) (cut + paste, in-commit or via `scripts/finalize_tech_debt_ledger.py`). The pair forms one logical namespace — see the rule for re-open semantics.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-002 | important | other | code-to-goals | claude/canonical-blocks/agent-pipeline.md | code-quality |  | orchestrator | 2026-04-27 | 2026-04-27 | implementation-planner | open |  | Agent-pipeline canonical block is ~1818 bytes (~505 tokens), exceeding per-block ~250-token budget by ~2x; pre-existing condition surfaced during td-001 refactor, not introduced by it. Trimming changes downstream injection contract — needs content review and separate ADR. Currently xfailed in `hooks/test_onboard_praxion_block.py::test_block_stays_within_token_budget[agent-pipeline]`. Filed by orchestrator mid-pipeline (slight protocol stretch — canonical sources are verifier and sentinel); verifier may re-source. Class `other` because no enum slot covers content-size-budget violations. | deee2e5a8b62 |
| td-003 | important | complexity | code-to-goals | memory-mcp/src/memory_mcp/store.py:1-947 | code-quality |  | sentinel | 2026-04-28 | 2026-04-28 | implementation-planner | open |  | `store.py` is 947 lines, exceeding the 800-line ceiling in `rules/swe/coding-style.md`. Suggested module split (per prior planning): extract auto-link logic, helper functions, and incoming-link scanning into separate modules. Module restructuring → owner-role = implementation-planner per heuristic. Migrated from retired ROADMAP §5.6 on 2026-04-28; depends on `dec-041` type-checking foundation already shipped. | a04e05874650 |
| td-004 | important | complexity | code-to-goals | task-chronograph-mcp/src/task_chronograph_mcp/otel_relay.py:1-1468 | code-quality |  | sentinel | 2026-04-28 | 2026-04-28 | implementation-planner | open |  | `otel_relay.py` is 1,468 lines (grew from 921 in earlier audit), nearly 2x the 800-line ceiling. Suggested module split: extract span creation methods to `span_factory.py`, agent context tracking to `context_tracker.py`. Worsening trend across recent telemetry work (`dec-052` duration-aware spans) compounds the excess. Module restructuring → owner-role = implementation-planner. Migrated from retired ROADMAP §5.6 on 2026-04-28. | f8ceab2e7268 |
| td-005 | important | other | code-to-goals | eval/pyproject.toml, eval/src/praxion_evals/cli.py, eval/src/praxion_evals/regression/ | code-quality |  | verifier | 2026-04-28 | 2026-04-28 | implementation-planner | open |  | Eval framework `regression` mode is shipped-but-broken: keys baselines by `task_slug` but Praxion slugs are one-shot (each feature → unique slug → cleaned up), so drift detection has no sample to compare against. Compounding bug: `eval/pyproject.toml` depends on `arize-phoenix-evals` but not `arize-phoenix`, so `phoenix.Client()` raises `AttributeError` swallowed by broad `except Exception` in `trace_reader.py` — live capture silently returns empty. Required redesign: tier/shape-keyed envelope baselines (p50/p95 across N pipelines per tier/shape) + chronograph `praxion.tier`/`praxion.shape.*` span attributes + fix the dep gap + surface Phoenix connectivity errors instead of swallowing. Class `other` because no enum slot covers "shipped-but-design-flawed". Owner-role = implementation-planner because it requires ADR (partial supersession of `dec-040` invocation discipline) plus chronograph schema change before code can land. Migrated from retired ROADMAP §3.7 on 2026-04-28. | 51fe8bd48f7a |
| td-012 | important | complexity | code-to-goals | scripts/project_metrics/report.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-13 | implementation-planner | open |  | Rank-2 hotspot (score=21,399): complexity=21, churn=1,019 over 90d; central reporting aggregator in active development; complexity trend warranted monitoring before it reached the 800-line ceiling. // 2026-05-13: re-severitied suggested→important and owner-role implementer→implementation-planner — `report.py` is now 929 lines, OVER the 800-line ceiling in `rules/swe/coding-style.md` it was being monitored toward (still rank-3 hotspot, score=21,399, in METRICS_REPORT_2026-05-13). Module split warranted — extract per-collector rendering subsections to a `report_sections/` package, or split aggregate/trend/hotspot rendering from the orchestration shell. Owner-role flipped per the "module restructuring → implementation-planner" heuristic, matching td-003 / td-004; revert to implementer if a modest extraction (vs. a full decomposition) turns out to suffice. | 62a324775090 |
| td-013 | suggested | complexity | code-to-goals | memory-mcp/src/memory_mcp/metrics.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-13 | implementer | open |  | Rank-4 hotspot (score=15,540): complexity=28 (second-highest in codebase after otel_relay.py); metrics module in active development; warrants restructuring consideration if complexity grows further. // 2026-05-13: de-escalated — `metrics.py` is no longer in the top-10 hotspots (churn dropped out of the 90d window per METRICS_REPORT_2026-05-13); now just a high-complexity-density file: 503 lines (well under the 800 ceiling), p95 CCN ~27 (still 2nd-highest in the codebase after otel_relay.py). Holding at `suggested`; close if the team accepts the current density, or re-escalate if complexity grows. | 8fe359ca6f92 |
| td-021 | important | other | code-to-goals | CLAUDE.md, rules/swe/swe-agent-coordination-protocol.md | code-quality |  | orchestrator | 2026-05-08 | 2026-05-08 | doc-engineer | open |  | Claude Code shipped `Explore` subagent (Claude Code 2.1.136) crashes during initialization with `undefined is not an object (evaluating 'K.length')` when spawned from a Praxion session. Reproducible: a 50-word minimal prompt (`Find /tmp/x, count lines, return first three`) fails identically — NOT prompt-size dependent. Failure mode: orphaned-tool-start in the chronograph observability panel — input context is packaged and billed (~55K tokens/call observed by operator on 2026-05-08), output never returns. Distinct from upstream issue #38868 (CLOSED, 2.1.83) which reported a graceful "Prompt is too long" / AxiosError surface for the same trigger condition (many plugins/skills/MCPs injecting context); our failure is a silent JS crash with no recoverable error message. Praxion `i-am:researcher` (3rd attempt in the same session) handled the same workload cleanly. Token cost (operator-reported, not directly visible in `.ai-state/observations.jsonl` schema). Resolution path: (a) document workaround in CLAUDE.md `Known Claude Code Limitations` (this commit) — prefer `i-am:researcher` for substantive inventory; (b) add note in `rules/swe/swe-agent-coordination-protocol.md` to scope when Explore is reasonable vs when to skip; (c) upstream bug filed at [anthropics/claude-code#57493](https://github.com/anthropics/claude-code/issues/57493) on 2026-05-08, referencing #38868, #43983, #30620. Distinct from td-018 (token-budget) — this is a hard JS crash, not a budget overflow, even though both surface in many-skills environments. | 2df31ac7a5ba |
| td-024 | suggested | coverage-gap | code-to-goals | dashboard_app/tests/ | code-quality |  | orchestrator | 2026-05-11 | 2026-05-11 | test-engineer | open |  | React component render tests (jsdom + `@testing-library/react`) deferred — no jsdom dep in v1. v1 covers server view-models + pure parsers/helpers + `usePanZoom` math via synthetic events. Resolution: `pnpm add -D jsdom @testing-library/react @testing-library/user-event` + vitest jsdom env config + tests for DiagramViewer, DecisionGraph, TrendChart, shells. User-confirmed defer 2026-05-11. | 70176a5a9742 |
| td-025 | suggested | other | code-to-goals | dashboard_app/src/components/shells/ | code-quality |  | orchestrator | 2026-05-11 | 2026-05-11 | implementation-planner | open |  | Dedicated shell chrome for `tutorial`, `how-to`, and `concepts` Diátaxis types deferred. Currently these three types alias `DefaultShell`. Resolution: build dedicated layout components (tutorial: step-numbered layout; how-to: goal + steps layout; concepts: narrative with key-takeaway callout) and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. | 69bd97ba8d95 |
| td-027 | suggested | other | code-to-goals | dashboard_app/src/components/registry.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-05-11 | implementation-planner | open |  | The Streamlit `metrics_view` / `plan_view` / `verification_report` / `idea_grid` / `architecture_explorer` per-artifact renderers have no Next.js equivalents yet. v1 ships `reference` + `explanation` + `default` Diátaxis shells only. Resolution: build dedicated renderer components and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. | 4ce09473665c |
| td-029 | suggested | other | code-to-goals | dashboard_app/src/server/view-models/documentation.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-05-11 | systems-architect | open |  | `assertContainedProjectPath` is intentionally wide for documentation surfaces (any file in project root). A compromised `doc_manifest.yaml` could expose `src/` or `.env` files. Resolution: add a secondary allowlist for doc surfaces restricting paths to known-safe subdirectories (e.g., `docs/`, `.ai-state/`). Class `other`: no enum slot covers security-hardening deferrals. | 70e11df29f5c |
| td-033 | important | other | code-to-goals | agents/doc-engineer.md, agents/implementer.md, agents/test-engineer.md, skills/rule-crafting/SKILL.md | code-quality |  | orchestrator | 2026-05-12 | 2026-05-12 | unassigned | open |  | Path-scoped rules (`paths:` frontmatter) inject only when Claude reads a matching file — not on Write/Edit/MultiEdit (verified 2026-05-12 against `code.claude.com/docs/en/memory` + GitHub issues anthropics/claude-code#23478, #38487, #16853). An agent that creates a new file via Write without first reading a matching sibling misses that file type's conventions: new `.py`/`.ts` misses `coding-style.md`; new doc misses `readme-style.md`/`diagram-conventions.md`/`html-output-conventions.md`/`aac-dac-conventions.md`; new `.github/*.md` misses `pr-conventions.md`; new files miss `id-citation-discipline.md`, `staleness-policy.md`. Touches Praxion's rules-as-guardrails guarantee surface directly; moderate untreated severity (agents usually read a directory before working in it, which incidentally loads the rule). Mitigated 2026-05-12 (ROADMAP P1): added a "read an existing sibling before creating a new file" instruction to the Constraints sections of `agents/implementer.md`, `agents/doc-engineer.md`, `agents/test-engineer.md`; documented symptom + full mitigation in `skills/rule-crafting/SKILL.md` (new "Path-Scoped Rules: Read-Only Loading Trigger" section); added a `Known Claude Code Limitations` bullet to CLAUDE.md; added a path-scoped caveat next to the `paths:` convention in `rules/CLAUDE.md`. // **2026-05-12 upstream re-check** — #23478 (core bug) CLOSED `NOT_PLANNED` 2026-03-14, #38487 (feature request) CLOSED `NOT_PLANNED` 2026-04-30: upstream has **explicitly declined** to load path-scoped rules on Write/Edit. The agent-prompt mitigation is therefore **permanent** (not provisional) and should be treated as a design constraint, not awaited tech debt. #16853 remains open with a relevant workaround signal: per user comments, multi-entry YAML-list `paths:` may fail under the `_9A()` CSV parser, while single-entry brace-expansion (`paths: ["**/*.{py,pyi}"]`) and inline-array form work consistently — Praxion's rules predominantly use YAML-block-list syntax, so `scripts/check_paths_syntax.py` was added 2026-05-12 to flag at-risk rule files. Windows variant #21858 CLOSED `COMPLETED` 2026-03-24 — no further Praxion action (won't-do); the existing Windows caveat in `docs/existing-project-onboarding.md` stays in place as historical guidance. Optional non-blocking follow-up: sandbox an `InstructionsLoaded` hook to log which path-scoped rules actually load in a pipeline run; lean against unless the prompt mitigation proves insufficient. Class `other`: no enum slot covers upstream-tool-behavior gaps (same as td-021). Owner-role `unassigned`: resolution is gated on an external upstream stance that won't change, so this row should be promoted to a "documented design constraint" (move to RESOLVED as `wontfix`) in a subsequent ledger sweep once the syntax-checker script confirms no at-risk rules remain. | b8fb1a83b357 |
