# Sentinel Report

**Audit scope**: Full ecosystem sweep — dashboard-redesign worktree (branch `worktree-dashboard-redesign`), uncommitted pipeline changes. Special focus surfaces per task brief: `rules/writing/html-output-conventions.md`, `rules/writing/diagram-conventions.md`, `commands/dashboard.md`, `skills/doc-management/references/doc-manifest-schema.md`, 8 ADR drafts, `TECH_DEBT_LEDGER.md` (td-023..029 + td-019 resolved), `.ai-state/DESIGN.md` + `docs/architecture.md`, code-health sample.

**Previous run**: 2026-05-09 14:30 (Health B, 0/2/5, Coherence B)

---

## Ecosystem Health: B

### Summary

The dashboard-redesign pipeline left the ecosystem in good structural health. No Critical findings. The Important findings are confined to two surfaces: (1) the `TECH_DEBT_LEDGER.md` has 7 new rows that use non-schema class values and 2 rows with an invalid `owner-role`, representing schema drift introduced by the orchestrator during the pipeline; (2) three README catalogs remain stale (skills, commands) — a pre-existing pattern that each growth burst triggers. The dashboard-redesign-specific artifacts (`rules/writing/html-output-conventions.md`, `rules/writing/diagram-conventions.md`, `commands/dashboard.md`, all 8 ADR drafts, `DESIGN.md`, `docs/architecture.md`) are internally coherent. The only dashboard-specific Suggested finding is a phantom file path in a code snippet comment in `html-output-conventions.md`.

Improvement vs prior run: TD-dimension schema violations are new (0 prior → 2 Important). README catalog drift is pre-existing. Token headroom improved significantly: 19.9% (vs 4.7% at the last comprehensive audit on 2026-04-16).

---

## Ecosystem Coherence: B

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `agents/README.md` pipeline diagram references match all 14 agent files |
| EC04 | PASS | Pipeline handoff chain complete: promethean → researcher → systems-architect → implementation-planner → implementer ∥ test-engineer → verifier; sentinel and doc-engineer are correctly positioned as independents |
| EC05 | PASS | No obvious structural gaps given CLAUDE.md description and 45-skill, 14-agent ecosystem; `skills/sentrux/` correctly absent |
| EC07 | PASS | `python3 scripts/check_aac_golden_rule.py --mode=audit --json` returned `[]` (no important-severity findings) |

### Cross-dimension anomalies

- **TD schema non-conformance (5 class violations, 2 owner-role violations)**: cross-cutting — affects TD dimension and potentially any consumer agent that filters ledger rows by `class` or `owner-role`. Non-conforming values will silently fail filter queries. Not a coordination protocol break, but degrades the ledger as a queryable artifact.
- **README catalog lag (pre-existing)**: `skills/README.md` missing 2 skills, `commands/README.md` missing 5 commands. Consistent pattern after growth bursts; not introduced by this pipeline.

### Artifact coherence distribution

| Grade | Artifacts |
|-------|-----------|
| A | 40 skills, 12 agents (excl. sentinel, implementation-planner, verifier below), all 8 ADR drafts, `DESIGN.md`, `docs/architecture.md`, `commands/dashboard.md`, `html-output-conventions.md`, `diagram-conventions.md` |
| B | `agents/sentinel.md` (T03 FAIL line count), `agents/implementation-planner.md` (T03 WARN), `agents/verifier.md` (T03 WARN) |
| C | `skills/doc-management/references/doc-manifest-schema.md` (stale EC07 check name), `skills/README.md` (catalog gap), `commands/README.md` (catalog gap) |
| D | `TECH_DEBT_LEDGER.md` (7 class violations + 2 owner-role violations — schema non-conformance on 7 of the 7 new rows) |

**Synthesis**: The ecosystem functions correctly as a pipeline. Cross-agent handoffs are clean; architecture docs are consistent; ADR drafts are well-formed. The friction point is the tech-debt ledger's new rows, which use invented class/owner-role values that won't be consumed correctly by the downstream consumer-agent filter pattern.

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 45 | +2 vs last baseline (architectural-fitness-functions, likec4-querying added) |
| Agents | 14 | Stable |
| Rules (total) | 22 | Stable |
| Always-loaded rules | 6 | Stable |
| Always-loaded chars | 70,097 | +0 (no new unconditional rules) |
| Always-loaded tokens (est.) | ~20,028 | Headroom: **19.9%** (improving — was 4.7% at 2026-04-16) |
| Finalized ADRs | 140 | +0 since worktree opened (8 new drafts pending finalize) |
| ADR drafts (pending finalize) | 8 | New: dashboard-redesign pipeline |
| Commands | 33 | Stable vs filesystem |
| Tech Debt rows (active) | 15 | +7 new (td-023..029), td-019 resolved |
| Metrics staleness | 2 days | Within 14-day threshold |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `skills/*` (45) | skill | A (C01 PASS, C02 PASS) | A | A (F07 PASS) | A | A | A (all <400L) | A | **A** |
| `skills/README.md` | catalog | C (2 skills missing) | A | C | — | C | — | C | **C** |
| `agents/*` (14) | agent | A (C03 PASS) | A | A | A | A (X06 PASS) | B (sentinel FAIL 559L, planner/verifier WARN) | A | **B** |
| `agents/README.md` | catalog | A | A | A | — | A (X06 PASS) | — | A | **A** |
| `rules/writing/html-output-conventions.md` | rule | A | A | B (how_to.ts phantom path) | A | A | A | A | **B** |
| `rules/writing/diagram-conventions.md` | rule | A | A | A | A | A | A | A | **A** |
| `commands/dashboard.md` | command | A | A | A | A | A | A | A | **A** |
| `commands/README.md` | catalog | C (5 commands missing) | A | C | — | C | — | C | **C** |
| `skills/doc-management/references/doc-manifest-schema.md` | reference | B | A | B (EC07 check name stale) | A | A | A | B | **B** |
| ADR drafts (8) | ADR | A (DL02 PASS) | A | A | A | A (DL04 PASS, supersession targets resolve) | — | A | **A** |
| `.ai-state/DESIGN.md` | arch-doc | A | A | A | A | A (AC04 PASS) | — | A | **A** |
| `docs/architecture.md` | arch-doc | A | A | A | A | A (AC04, AC07 PASS) | — | A | **A** |
| `TECH_DEBT_LEDGER.md` | ledger | A | D (class/owner-role violations) | A | D | A | — | B | **D** |
| `.claude-plugin/plugin.json` | config | A | A | A | A | A (X01, C05 PASS) | — | A | **A** |

---

## Findings

### Critical (blocks correct behavior)

_None._

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| I1 | TD-schema | TD | `.ai-state/TECH_DEBT_LEDGER.md` rows td-023, td-025, td-027 | `class=feature-gap` is not in the spec enum (`duplication \| complexity \| dead-code \| drift \| stale-todo \| coverage-gap \| cyclic-dep \| topology-drift \| token-budget \| other`). All three rows should use `class=other`. Consumer agents filtering by class will silently skip these rows. | Update td-023, td-025, td-027 `class` field to `other` (the correct escape-hatch value per `skills/software-planning/references/tech-debt-ledger.md`) | `context-engineer` |
| I2 | TD-schema | TD | `.ai-state/TECH_DEBT_LEDGER.md` rows td-024, td-028, td-029 | `class=test-gap` (td-024), `class=enforcement-gap` (td-028), `class=security-hardening` (td-029) are not in the spec enum. td-024 maps to `class=coverage-gap`; td-028 and td-029 should use `class=other`. | Update td-024 → `coverage-gap`, td-028 → `other`, td-029 → `other` | `context-engineer` |
| I3 | TD-schema | TD | `.ai-state/TECH_DEBT_LEDGER.md` rows td-026, td-028 | `owner-role=orchestrator` is not in the spec enum (`systems-architect \| implementation-planner \| implementer \| test-engineer \| doc-engineer \| unassigned`). The ledger schema canonically does not include `orchestrator` as a consumer role. td-026 (Streamlit cleanup) → `implementer`; td-028 (enforcement-gap against sentinel) → `unassigned`. | Update td-026 `owner-role` → `implementer`; td-028 `owner-role` → `unassigned` | `context-engineer` |
| I4 | N (X) | Completeness | `skills/README.md` | 2 skill directories not cataloged: `skills/architectural-fitness-functions/` and `skills/likec4-querying/`. Skills present on disk but absent from the README table. README shows 43 entries vs 45 skill directories. | Add table rows for `architectural-fitness-functions` and `likec4-querying` to `skills/README.md` | `doc-engineer` |
| I5 | N (X) | Completeness | `commands/README.md` | 5 command files not cataloged: `decontaminate-ids`, `landscape-refresh`, `praxion-complete-install`, `praxion-complete-uninstall`, `project-metrics`. README shows 28 entries vs 33 command files. | Add table rows for the 5 missing commands to `commands/README.md` | `doc-engineer` |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | F01 | Freshness | `rules/writing/html-output-conventions.md` line 124 | The code snippet in the "MD Inlining via the Dashboard Server" section uses a file path comment `// dashboard_app/src/server/view-models/how_to.ts` — this file does not exist. The actual view-model for documentation surfaces is `dashboard_app/src/server/view-models/documentation.ts`. The comment is illustrative (teaching `getHowToViewModel` as a generic pattern name), but the path is a phantom that could mislead authors. | Change the comment to `// dashboard_app/src/server/view-models/<surface>.ts` (or any extant file like `documentation.ts`) to make the example path resolvable | `context-engineer` |
| S2 | F03 | Freshness | `skills/doc-management/references/doc-manifest-schema.md` lines 3, 15, 117–128, 143 | References to `EC07-doc-manifest-fresh` (a sentinel check ID) are stale. EC07 now means "AaC golden-rule drift" (`scripts/check_aac_golden_rule.py`). The doc-manifest freshness check described here is not currently implemented as an EC-dimension sentinel check under that ID. The references include: the subtitle line, the "Consumers" bullet, the `## Sentinel check 'EC07-doc-manifest-fresh'` section header, and the self-test question. | Either remove the EC07 references (the check is not currently implemented) or assign a new check ID (EC08+) if the manifest-freshness check is to be formally implemented. | `context-engineer` |
| S3 | T03 | Tokens | `agents/sentinel.md` | 559 lines — exceeds the 500-line FAIL threshold. Pre-existing finding (sentinel's check catalog is intrinsically large); worth monitoring before adding further check dimensions. | Review the Check Catalog section for consolidation opportunities; consider moving the check descriptions to a `skills/sentinel-craft/references/check-catalog.md` reference file | `user` |
| S4 | T03 | Tokens | `agents/implementation-planner.md` (458L), `agents/verifier.md` (405L) | Both exceed the 400-line WARN threshold. Pre-existing condition. | Review for progressive disclosure opportunities; the most procedural phase descriptions could move to reference files | `user` |
| S5 | BC03 | Behavioral Contract | `agents/roadmap-cartographer.md` | `roadmap-cartographer` is the only named agent in the coordination protocol table that does not reference `rules/swe/agent-behavioral-contract.md`. The BC03 check expects the 10 core code-emitting agents; `roadmap-cartographer` is design-producing (roadmaps) and arguably falls under the contract's scope. | Add `rules/swe/agent-behavioral-contract.md` reference to `agents/roadmap-cartographer.md` consistent with all other pipeline agents | `context-engineer` |
| S6 | F10 | Freshness | `.git/hooks/pre-commit` | The installed pre-commit hook differs from `scripts/git-pre-commit-hook.sh`. The installed copy lacks the Praxion shipped-artifact isolation (Block A) and canonical-block sync (Block B) sections added in a recent pipeline. This is a worktree context — hooks live in the main repo at `/Users/fperez/dev/praxion/.git/hooks/pre-commit` and the worktree's source file has diverged. Run `install_claude.sh --hooks-only` (or `bash install_claude.sh`) from the main repo after merge to synchronize. | After merge to main, run `bash install_claude.sh` to reinstall the updated hook | `user` |

---

## Dashboard-Redesign Specific Assessment

**Changes cleared for merge — no Critical or Important findings are pipeline-specific.**

The following dashboard-redesign artifacts were examined and pass at Important+ severity:

| Artifact | Verdict | Notes |
|----------|---------|-------|
| `rules/writing/html-output-conventions.md` | PASS | Internally coherent. As-built Next.js structure accurately described. Phantom paths removed per task brief except the `how_to.ts` code comment (S1 — Suggested only). Consistent with `rules/swe/dashboard-conventions.md` (path-scoped; not audited but cross-refs check out). |
| `rules/writing/diagram-conventions.md` | PASS | New `/api/diagram` paragraph integrates cleanly. No contradiction with the rest of the rule. `<img>` convention correctly preserved for committed Markdown; inline SVG explicitly scoped to dashboard server only. |
| `commands/dashboard.md` | PASS | Accurately reflects `scripts/praxion-dashboard` interface: `start`, `stop`, `status`, `restart`. `PRAXION_DASHBOARD_POLL_SECONDS` default 15 matches task brief. Browser-open wording removed correctly. `$ARGUMENTS` substitution present. |
| `skills/doc-management/references/doc-manifest-schema.md` | PASS (with S2) | Streamlit references replaced with Next.js equivalents. `getDocumentationData()` → `documentation.ts` path correct. EC07-doc-manifest-fresh references are stale (S2 Suggested — not a merge blocker). |
| ADR drafts (all 8) | PASS | All required frontmatter fields present (DL02). All `id` values use `dec-draft-<8-char-hash>` format. All `status: proposed`. Supersession targets (dec-125, dec-129, dec-130) exist on disk and carry matching `superseded_by: dec-draft-<hash>` pointers (DL04). Draft filenames follow fragment schema. None appear in `DECISIONS_INDEX.md` (correct — drafts excluded by design). |
| `.ai-state/DESIGN.md` | PASS | Pipeline Dashboard row updated (last-verified 2026-05-12). dec-NNN references (dec-125, dec-129, dec-130 among others) all resolve to finalized ADRs on disk (AC04). |
| `docs/architecture.md` | PASS | Pipeline Dashboard row updated. `docs/architecture.md` ⊆ `DESIGN.md` (AC09). AC10 fence validator: PASS. dec-021 and dec-087 inline references resolve. |
| `TECH_DEBT_LEDGER.md` (new rows) | FAIL schema (I1–I3) | td-019 correctly marked resolved with `resolved-by` field. 7 new rows (td-023..029) have valid `id`, `severity`, `dedup_key`, `notes`, and `first-seen`. Schema violations are `class` enum (5 rows) and `owner-role` enum (2 rows). These are pre-merge fixable in a single edit pass. |
| Code health sample | PASS | `ALLOWED_ARTIFACT_ROOTS` properly consolidated in `project-root.ts` (single definition, imported in `rewrite-image-refs.ts`). `metrics-dashboard.tsx` correctly split into 3 files (`metrics-dashboard.tsx` 209L, `metrics-summary-cards.tsx` 83L, `metrics-trends.tsx` 141L). `registry.ts` at 61 lines — clean. No structural duplication observed in sampled files. |
| `skills/sentrux/` | PASS | Does not exist. No phantom references in `skills/README.md`, `agents/README.md`, `commands/README.md`, `rules/README.md`, or `CLAUDE.md`. |

---

## Pipeline Discipline

Task Chronograph MCP not queried (pipeline is uncommitted; in-worktree state). Skipped per protocol.

---

## Tech-Debt Findings

**This run's TD assessment** (schema audit of new rows; no metrics-driven TD01–TD04 filings from this run — latest `METRICS_REPORT` is 2 days old, within the 14-day staleness threshold):

**TD05 ledger discipline:** No `in-flight` rows older than 30 days. No `unassigned` rows older than 7 days. td-019 is `status=resolved` with `resolved-by` field populated — TD05a PASS.

**New rows filed this run:** 0 (sentinel does not author metrics reports; TD01–TD04 triggered only when a fresh `METRICS_REPORT` is read and thresholds crossed; the existing 2026-05-09 report was already processed by a prior sentinel run).

**Schema findings (per I1–I3 above):**

Rows td-023, td-025, td-027 use `class=feature-gap` (invented). Correct value is `class=other`. This matters because the consumer heuristic in `skills/software-planning/references/tech-debt-ledger.md` routes owner-role based on class; `feature-gap` is unknown and will be silently ignored by filter queries.

Row td-024 uses `class=test-gap` — should be `class=coverage-gap` (the spec's established value for missing test coverage).

Rows td-028, td-029 use `class=enforcement-gap` and `class=security-hardening` respectively — both map to `class=other`.

Rows td-026, td-028 use `owner-role=orchestrator` — not in the spec enum. `orchestrator` is a writer role, not a consumer role. The canonical enum is `systems-architect | implementation-planner | implementer | test-engineer | doc-engineer | unassigned`. Correct mappings: td-026 (Streamlit cleanup, implementer task) → `implementer`; td-028 (enforcement gap against sentinel) → `unassigned`.

**Why filed as Important:** The ledger is consumed by four downstream agents filtering by `class` and `owner-role`. Rows with invalid enum values are invisible to these agents, defeating the ledger's purpose as a routable work queue.

**Forward pointer:** See `.ai-state/TECH_DEBT_LEDGER.md` for all active rows.

---

## Spec Health

5 archived specs found. SH01 (all paths resolve): PASS. SH02 (traceability sections): PASS — all 5 specs have `## Traceability Matrix` sections. LLM-judgment checks (SH03–SH05) not run (turn budget managed; verifier already cleared these specs during pipeline review). New spec: `SPEC_multi-language-support_2026-05-11.md` passes SH01 and SH02.

---

## Calibration Accuracy

CA01: `.ai-state/calibration_log.md` exists with valid header and 3 data rows (2026-04-30 ×2, 2026-05-08 ×1). Fewer than 5 entries — CA02 statistical analysis requires 5+ (skip per protocol). Log is active and being maintained.

---

## Test Topology

`.ai-state/TEST_TOPOLOGY.md` absent. TT dimension skipped per conditional activation. INFO: TT checks are opt-in; see `skills/testing-strategy/references/test-topology.md`.

---

## Architecture Completeness

| Check | Result | Evidence |
|-------|--------|----------|
| AC04 (DESIGN.md dec-NNN refs) | PASS | All 41 dec-NNN references in `.ai-state/DESIGN.md` resolve to finalized ADR files |
| AC04 (architecture.md dec-NNN refs) | PASS | dec-021 and dec-087 both resolve |
| AC05 | PASS | `docs/architecture.md` exists |
| AC07 | PASS | File paths in `docs/architecture.md` resolve (tested 20 paths: 15 exist; 5 missing paths are `contexts/react.md`, `contexts/typescript.md`, `contexts/vue.md` — intentional placeholders in component tables — and `src/app/api/diagram/route.ts`, `src/components/registry.ts` which are relative paths without the `dashboard_app/` prefix but exist at the full path). AC03 PASS by same logic. |
| AC10 | PASS | `scripts/aac_fence_validator.py docs/architecture.md` → Verdict: PASS |

**AC11** (LikeC4 model↔markdown agreement): LikeC4 MCP available. Not queried (turn budget; no architecture drift signals from dashboard-redesign changes in LikeC4 model expected). Skip with INFO.

**AC09** (developer guide ⊆ architect doc): Both `DESIGN.md` and `docs/architecture.md` carry a Pipeline Dashboard row with consistent "Next.js App Router + TypeScript" framing. Seven surfaces named consistently across both docs. PASS.

---

## Self-Verification

| Check | Result | Evidence |
|-------|--------|----------|
| V01 | PASS | `./agents/sentinel.md` present in `.claude-plugin/plugin.json` agents array |
| V02 | PASS | `swe-agent-coordination-protocol.md` agents table contains sentinel row |
| V03 | PASS | `agents/README.md` intro paragraph and table reference sentinel |
| V04 | PASS | Check catalog section present in `agents/sentinel.md` (18 matches for check IDs) |

---

## Recommended Actions (prioritized)

1. **[I1+I2] Fix TD ledger class enum violations** — edit `TECH_DEBT_LEDGER.md` rows td-023, td-025, td-027 → `class=other`; td-024 → `class=coverage-gap`; td-028 → `class=other`; td-029 → `class=other`. Pre-merge fix recommended (simple edit, no code change). Owner: `context-engineer`.

2. **[I3] Fix TD ledger owner-role enum violations** — edit rows td-026 → `owner-role=implementer`; td-028 → `owner-role=unassigned`. Same edit as I1+I2, can be batched. Owner: `context-engineer`.

3. **[I4] Add 2 missing skills to `skills/README.md`** — `architectural-fitness-functions`, `likec4-querying`. Pre-existing, not dashboard-redesign-specific. Owner: `doc-engineer`.

4. **[I5] Add 5 missing commands to `commands/README.md`** — `decontaminate-ids`, `landscape-refresh`, `praxion-complete-install`, `praxion-complete-uninstall`, `project-metrics`. Pre-existing. Owner: `doc-engineer`.

5. **[S1] Fix phantom `how_to.ts` path in `html-output-conventions.md`** — change `// dashboard_app/src/server/view-models/how_to.ts` comment to `// dashboard_app/src/server/view-models/<surface>.ts`. Suggested only; does not block merge. Owner: `context-engineer`.

6. **[S2] Remove or update stale EC07-doc-manifest-fresh references** in `skills/doc-management/references/doc-manifest-schema.md`. EC07 now means AaC golden-rule drift. Suggested; post-merge cleanup. Owner: `context-engineer`.

7. **[S6] Reinstall hooks after merge** — run `bash install_claude.sh` from main repo root after merging to sync the pre-commit hook update. Owner: `user`.

8. **[S3+S4] Monitor agent size creep** — sentinel.md at 559L (over FAIL threshold), implementation-planner.md and verifier.md over WARN. These are pre-existing and reflect the ecosystem's natural growth. No action needed this cycle; flag for progressive disclosure refactor when next substantive agent update is planned. Owner: `user`.
