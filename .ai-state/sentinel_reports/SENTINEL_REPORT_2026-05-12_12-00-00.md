# Sentinel Report

**Generated:** 2026-05-12 12:00:00
**Audit scope:** Full ecosystem sweep — all dimensions — post-interface-designer change, worktree `interface-design`
**Commits since last audit (main baseline):** 5+ (after `SENTINEL_REPORT_2026-05-08_14-30-00.md`)
**Worktree state:** `interface-designer` feature branch, verified PASS by verifier (ready-to-merge)
**Focus:** 10-dimension audit with per-finding `[pre-existing]` / `[introduced]` tags

---

## Ecosystem Health: B

### Summary

The ecosystem is in **good shape.** The `interface-designer` change is structurally sound — the new agent, four skills, pipeline wiring, coordination-protocol additions, ADR fragments, and README updates are all coherent and correctly cross-referenced. No Critical findings. Five Important findings total (1 pre-existing, 4 introduced), none of which block correctness.

The headline introduced findings are:

1. **BC03 stale** — the sentinel's own check catalog still names "exactly 10 code-emitting agents" and lists them by name; `interface-designer` is now an 11th code-emitting agent and should be added. The agent body has the behavioral contract text but omits the canonical file-path citation (`rules/swe/agent-behavioral-contract.md`) that BC03's grep searches for — so BC03 will misfire on every future run until both are fixed.
2. **`docs/architecture.md` counts stale** — the Skills row still says "35 skills on disk (2026-04-16)" and enumerates only the ML additions. Now 49 skills are on disk. The Agents row does not name `interface-designer`. This is an AC07 WARN (counts and key-file listing stale), distinct from the pre-existing AC07 broken hook ref.
3. **F07 cold-start WARNs** — `api-design-craft` and `agentic-interface-design` have `staleness_sensitive_sections:` frontmatter pointing to sections inside their reference files, but those reference files have no `<!-- last-verified: -->` markers yet. Per cold-start semantics, these are expected WARNs on first audit after policy adoption.

**Trend vs. last report (2026-05-08, grade B, 0/2/5):** Roughly stable. The two pre-existing Important findings from the 2026-05-08 report are still open (git hook drift AC07 + docs/architecture.md hook-name broken ref). The interface-designer change adds 1 Important (BC03 catalog staleness) and 1 Important (architecture doc counts), balanced by the fact that all ecosystem-coherence checks PASS.

**Token budget:** Verifier measured 81,067 chars ≈ 22,519 tokens (÷3.6) / 20,267 (÷4.0). This audit independently measured 73,341 chars ≈ 20,955 tokens (÷3.5) for the 9-file always-loaded set (global CLAUDE.md + project CLAUDE.md + rules CLAUDE.md + 6 unconditional rules). Discrepancy reflects different file sets. Both are comfortably under the 25,000-token ceiling. Headroom: ~4,045 tokens (16.2%) on the conservative ÷3.5 measurement. No breach.

---

## Ecosystem Coherence: A

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Pipeline diagram SVG re-rendered on 2026-05-12; `agents/README.md` table includes `interface-designer`; 15 agents in table matches 15 agent files |
| EC02 | PASS | All 49 skills are referenced: 4 new skills referenced by `interface-designer` (directly via `skills:` frontmatter) + `implementer`, `verifier`, `promethean` (wired in this change). No orphaned skills. |
| EC04 | PASS | Complete handoff coverage: `interface-designer` produces `INTERFACE_DESIGN.md` → consumed by `implementation-planner` (reads at step-decomposition) → `implementer` (builds sketched designs) → `verifier` (checks against `design-review-checklist.md`). No dead ends. |
| EC05 | PASS | No structural gaps. The interface-designer fills a documented capability gap (interface-layer design authority) identified in earlier pipeline audits. |
| EC06 | PASS | Condensed deliverables block in `~/.claude/CLAUDE.md` (4-agent scope: systems-architect, implementation-planner, implementer, verifier) matches `coordination-details.md` Delegation Checklists. `interface-designer` is intentionally outside this block's 4-agent scope. |
| EC07 | PASS | `scripts/check_aac_golden_rule.py --mode=audit --json` returned `[]` (no important-severity findings) |

### Cross-dimension anomalies

- **AC07 / docs/architecture.md stale counts `[pre-existing]`**: `scripts/git-post-merge-hook.sh` is referenced 4× and does not exist; Skills row says "35 skills (2026-04-16)" when 49 are on disk; Agents row does not reflect `interface-designer`.
- **BC03 stale catalog `[introduced]`**: sentinel's check catalog says "exactly 10 code-emitting agents" — `interface-designer` is now an 11th. The agent body text describes the behavioral contract but does not contain the file-path citation `rules/swe/agent-behavioral-contract.md` that BC03's grep targets — causing BC03 to both miscount and misidentify on future runs.
- **F07 cold-start `[introduced]`**: `api-design-craft` (4 sections) and `agentic-interface-design` (1 section) have `staleness_sensitive_sections:` entries pointing to reference-file sub-sections with no `<!-- last-verified: -->` markers yet. By design (cold-start semantics); WARN only, not FAIL.

### Artifact coherence distribution

| Grade | Count | Notes |
|-------|-------|-------|
| A | 48 | All new skills, new agent, updated consumer agents, all rules, all commands |
| B | 1 | `docs/architecture.md` (pre-existing AC07 broken hook ref + introduced stale skill/agent counts) |

**Synthesis:** The ecosystem functions as a connected whole. The `interface-designer` integrates cleanly into the pipeline: wired into plugin.json, Available Agents table, Proactive Agent Usage section, the Tier Table (opus), the ADR "Who Writes" table, `INTERFACE_DESIGN.md` lifecycle (agent-intermediate-documents), and the coordination-details.md challenge-loop protocol. Bidirectional collaboration references are present: `systems-architect.md` references `interface-designer`, `implementation-planner` reads `INTERFACE_DESIGN.md`, `verifier` checks against it and has all 4 skills injected. The only coherence friction is the single file (`docs/architecture.md`) that carries stale counts and a broken path reference — both pre-existing issues that grew slightly worse with this change.

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 49 | ↑ (was 35 at last architecture doc update, 42 at last audit baseline, +4 this change) |
| Agents | 15 | ↑ (was 14) |
| Commands | 35 | ↑ (was 34; +1: `/review-interface`) |
| Rules (unconditional) | 7 | stable |
| ADR drafts | 7 | ↑ (all new, from this pipeline) |
| ADR finalized | 140 | stable |
| Always-loaded chars | ~73,341 | ↑ (+~1,983 chars from 4 rule edits) |
| Always-loaded tokens (÷3.5) | ~20,955 | ↑ (headroom: 4,045 / 16.2%) |
| Reference files | ~112 (est.) | ↑ (+26 new reference files from 4 new skills) |
| T03 agent prompt oversize (warn 300, fail 500) | 5 WARN, 1 FAIL | stable (pre-existing) |
| Ecosystem Health | B | → (stable vs prior B) |
| Ecosystem Coherence | A | ↑ (prior A; interface-designer adds coherence) |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `agents/interface-designer.md` | Agent | A | A | A | B | A | A | A | **A** |
| `skills/web-ui-design/` | Skill | A | A | A | A | A | A | A | **A** |
| `skills/tui-design/` | Skill | A | A | B | A | A | A | A | **B** |
| `skills/agentic-interface-design/` | Skill | A | A | B | A | A | A | A | **B** |
| `skills/api-design-craft/` | Skill | A | A | B | A | A | A | A | **B** |
| `commands/review-interface.md` | Command | A | A | A | A | A | A | A | **A** |
| `agents/systems-architect.md` (edited) | Agent | A | A | A | A | A | B | A | **A** |
| `agents/implementer.md` (edited) | Agent | A | A | A | A | A | A | A | **A** |
| `agents/verifier.md` (edited) | Agent | A | A | A | A | A | B | A | **A** |
| `agents/promethean.md` (edited) | Agent | A | A | A | A | A | A | A | **A** |
| `agents/implementation-planner.md` (edited) | Agent | A | A | A | A | A | B | A | **A** |
| `rules/swe/swe-agent-coordination-protocol.md` (edited) | Rule | A | A | A | A | A | A | A | **A** |
| `rules/swe/agent-model-routing.md` (edited) | Rule | A | A | A | A | A | A | A | **A** |
| `rules/swe/agent-intermediate-documents.md` (edited) | Rule | A | A | A | A | A | A | A | **A** |
| `docs/architecture.md` (not updated) | Doc | A | A | C | A | C | A | C | **C** |

Dimensions not shown in scorecard (pipeline + cross-cutting): Pipeline Discipline skipped (Chronograph not queried — out of scope for pre-merge audit). TT: `TEST_TOPOLOGY.md` absent — skipped (INFO). Code Health: not sampled this pass (no implementation code changes). Self-Verification V01-V04: all PASS.

---

## Findings

### Critical (blocks correct behavior)
*None.*

---

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | `[pre-existing]` / `[introduced]` | Recommended Action | Owner |
|---|-------|-----------|----------|---------|----------------------------------|-------------------|-------|
| I1 | BC03 | Behavioral Contract | `agents/interface-designer.md` line 62; `agents/sentinel.md` line 257 | **Two-part issue.** (a) `interface-designer.md` states the behavioral contract in prose ("Behavioral contract: surface assumptions…") but does not contain the canonical file-path citation `rules/swe/agent-behavioral-contract.md` that BC03's grep targets. Every other pipeline agent uses the pattern `Apply the behavioral contract (\`rules/swe/agent-behavioral-contract.md\`)`. (b) The BC03 check in the sentinel's own catalog names "exactly the 10 code-emitting agents" and does not include `interface-designer`. With 15 agents now on disk, BC03's "exactly 10" guard will produce a false-positive WARN on every future run until the catalog is updated to 11. | `[introduced]` | (a) Add one line to `agents/interface-designer.md`: `**Apply the behavioral contract** (\`rules/swe/agent-behavioral-contract.md\`): surface assumptions, register objections, stay surgical, simplicity first.` (b) Update BC03 in `agents/sentinel.md` to say "exactly the 11 code-emitting agents" and append `interface-designer` to the named list. | `context-engineer` |
| I2 | AC07 | Architecture Completeness | `docs/architecture.md` lines 48, 50, 64–66 | **Stale component counts and missing key-file citations introduced by this change.** Skills row says "35 skills on disk (2026-04-16)" — actual count is 49 (this change added 4 more; last count was 42 at the Apr-16 audit). Agents row does not name `interface-designer` or cite its file. This is an additive staleness on top of the pre-existing I3 finding below. | `[introduced]` | Update Skills row: bump count to 49, add `web-ui-design`, `tui-design`, `agentic-interface-design`, `api-design-craft` to "recent additions". Update Agents row: add `interface-designer` to key files list and bump the change note. | `doc-engineer` |
| I3 | AC07 | Architecture Completeness | `docs/architecture.md` lines 64, 66, 88 | **Broken file path references.** `scripts/git-post-merge-hook.sh` is cited 4× but the actual file is `scripts/git-finalize-hook.sh`. Every developer using the guide to understand the post-merge pipeline will follow a dead link. This is a pre-existing AC07 finding (first raised in the 2026-05-08 report) that was not resolved before this merge. | `[pre-existing]` | Replace every occurrence of `scripts/git-post-merge-hook.sh` with `scripts/git-finalize-hook.sh` in `docs/architecture.md`. | `doc-engineer` |
| I4 | F10 | Freshness | `.git/hooks/` | **Git hooks not installed in worktree.** `scripts/git-finalize-hook.sh` and `scripts/git-pre-commit-hook.sh` are both executable source files, but `.git/hooks/finalize` and `.git/hooks/pre-commit` are absent from this worktree's Git directory (`/Users/fperez/dev/praxion/.git/worktrees/interface-design`). Hooks that fire on commit/merge will not run — the ADR finalize and pre-commit checks are silently bypassed. Note: shared worktrees may inherit the parent `.git/hooks/` depending on Git version — verify on merge. | `[pre-existing]` (worktree-scoped; identical finding raised in prior reports on the main checkout) | Run `bash install.sh --hooks-only` (or `bash install.sh`) from the worktree root, or confirm that the parent `.git/hooks/` is shared. Ensure finalize hook runs at merge-to-main. | `user` |
| I5 | F07 | Freshness | `skills/api-design-craft/SKILL.md` lines 5–12; `skills/agentic-interface-design/SKILL.md` lines 5–8 | **Cold-start staleness WARNs: 5 sections cataloged, 0 markers.** `api-design-craft` declares 4 staleness-sensitive sections in `staleness_sensitive_sections:` (RFC 9457/RFC 9110 status, Relay Connection spec, OpenAPI 3.1, IETF RateLimit header draft — all pointing to sub-sections in `references/api-canon.md` and `references/rest-patterns.md`). `agentic-interface-design` declares 1 (token economy numbers in `references/progressive-disclosure-of-tools.md`). None of those reference file sections carry a `<!-- last-verified: -->` marker. Per cold-start semantics this is expected on first audit, but the markers should be added promptly so F08 age tracking kicks in. | `[introduced]` | Add `<!-- last-verified: 2026-05-12 -->` markers below the relevant headings in `skills/api-design-craft/references/api-canon.md` (RFC 9457, Relay, OpenAPI 3.1 sub-sections) and `references/rest-patterns.md` (IETF RateLimit sub-section), and in `skills/agentic-interface-design/references/progressive-disclosure-of-tools.md` (token economy section). | `context-engineer` |

---

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | `[pre-existing]` / `[introduced]` | Recommended Action | Owner |
|---|-------|-----------|----------|---------|----------------------------------|-------------------|-------|
| S1 | F07 | Freshness | `skills/tui-design/SKILL.md` lines 18–19 | **Non-standard staleness frontmatter format.** `staleness_sensitive_sections:` entries embed the date in the section-name string (e.g., `"TUI Framework Comparison (last-verified: 2026-05-12, review threshold: 120 days)"`) rather than using the canonical heading name + in-file `<!-- last-verified: -->` comment. The markers live at the top of the reference files (`tui-frameworks.md` L3, `cli-output-and-ux.md` L3) rather than below the headings in SKILL.md. The sentinel's F07 regex matches heading-level sections in SKILL.md — this placement causes F07 to misfire (section names don't match any SKILL.md heading). Not a policy violation, but the format deviates from `rules/swe/staleness-policy.md` spec and will cause false WARNs on future automated checks. | `[introduced]` | Normalize `staleness_sensitive_sections:` entries to bare section names: `"TUI Framework Comparison"` and `"clig.dev CLI Contract"`. Add `<!-- last-verified: 2026-05-12 -->` markers directly below the `## TUI Framework Comparison` heading in `references/tui-frameworks.md` and the `## The clig.dev Principles (Full Distillation)` heading (or equivalent) in `references/cli-output-and-ux.md`. | `context-engineer` |
| S2 | T03 | Token Efficiency | `agents/sentinel.md` (559 lines), `agents/implementation-planner.md` (459 lines), `agents/verifier.md` (413 lines), `agents/context-engineer.md` (358 lines), `agents/systems-architect.md` (395 lines) | **Agent prompt oversize (pre-existing).** Five agents exceed the 300-line WARN threshold; `sentinel.md` exceeds the 500-line FAIL threshold at 559 lines. This predates the current change — no new oversize agents were introduced. | `[pre-existing]` | Evaluate progressive-disclosure opportunities: move deep reference text in `sentinel.md` (check catalog prose) to a skill reference. Lower priority than I-series findings. | `context-engineer` |
| S3 | SH01 | Spec Health | `.ai-state/specs/SPEC_multi-language-support_2026-05-11.md` | **Spec references missing files.** `skills/python-development/references/python-resources.md` and `skills/python-development/contexts/python.md` are cited in the spec but don't exist on disk. The skill directory is `skills/python-prj-mgmt/`, not `skills/python-development/`. These are either planned-but-not-yet-created artifacts or a naming error in the spec. | `[pre-existing]` | Verify whether a `skills/python-development/` directory is planned or if the spec should reference `skills/python-prj-mgmt/` paths. Update the spec accordingly. | `context-engineer` |
| S4 | SH01 | Spec Health | `.ai-state/specs/SPEC_design-dialectic_2026-04-17.md` | **Spec references `references/design-synthesis.md`** (bare path). The actual file is at `skills/software-planning/references/design-synthesis.md`. Relative path in spec resolves incorrectly when the spec is read from `.ai-state/specs/`. | `[pre-existing]` | Update the path reference in the spec to either an absolute path or `../skills/software-planning/references/design-synthesis.md`. | `context-engineer` |
| S5 | CA02 | Calibration Accuracy | `.ai-state/calibration_log.md` | **Only 4 data rows** (header + 4 entries). CA02 requires ≥5 entries for analysis. Cannot produce recommendation-vs-actual match rate. Log remains below the analysis threshold. | `[pre-existing]` | Append tier-selection log entries consistently at pipeline completion, per `swe-agent-coordination-protocol.md`. This log will naturally grow; no action required unless teams want to force-populate it. | `user` |
| S6 | TD05 | Technical Debt | `.ai-state/TECH_DEBT_LEDGER.md` | **`td-021` owner-role is `doc-engineer`** with status `open`, first-seen 2026-05-08 (4 days ago). The item describes a Claude Code JS crash with a resolution path that includes: (a) CLAUDE.md update (done in-flight per LEARNINGS.md), (b) coordination-protocol note (done), (c) upstream bug filed. The ledger row may be stale — resolution steps (a) and (b) appear to be complete. Verify and migrate to TECH_DEBT_RESOLVED.md if fully resolved. | `[pre-existing]` | Verify against `LEARNINGS.md` whether steps (a)–(c) are complete. If complete, update `td-021` status to `resolved`, add `resolved-by` reference, and migrate to `TECH_DEBT_RESOLVED.md`. | `doc-engineer` |
| S7 | EC03 | Ecosystem Coherence | `agents/verifier.md` | **Verifier does not reference `interface-designer` by name** in its collaboration sections, despite now consuming `INTERFACE_DESIGN.md` and having the 4 interface-design skills injected. The consumption protocol is documented correctly (line 164–168: "When the task involved an interface surface..."), but there is no explicit bidirectional collaboration bullet pointing back to `interface-designer`. By contrast, `systems-architect` has a named `### With the Interface Designer` section. Not a functional gap (the consumption logic is present), but a coherence convention inconsistency. | `[introduced]` | Add a brief `## Collaboration — Interface Designer` note in `agents/verifier.md` referencing that `INTERFACE_DESIGN.md` is consumed and the four hat-skill checklists are applied. | `context-engineer` |

---

## Pipeline Discipline

Skipped: Task Chronograph not queried for this pre-merge audit. The pipeline's `PROGRESS.md` at `.ai-work/interface-design/PROGRESS.md` is present and populated (Full-tier pipeline lifecycle visible).

---

## Tech-Debt Findings

**TD01–TD04 (new rows):** Metrics last run 2026-05-09 (3 days ago, well under the 14-day staleness threshold). No new TD rows warranted this pass. The top hotspots from the 2026-05-09 metrics report are unchanged (`otel_relay.py` 1,468 lines, `store.py` 947 lines — both have open `td-003`/`td-004` ledger rows). No new cyclic SCCs introduced by this change (no Python implementation code modified).

**TD05 (ledger audit):** 9 active rows. `td-021` (doc-engineer, status open, 4 days old) has its primary resolution steps complete — surfaced as S6. No `owner-role = unassigned` rows. No `in-flight` rows older than 30 days. Ledger is healthy.

---

## Recommended Actions (prioritized)

1. **[I1] Add BC rule file-path citation to `interface-designer.md` + update BC03 sentinel catalog** — one-line edit to the agent, two-line edit to the sentinel's check table. Must be done before next automated sentinel run to prevent false-positive WARNs. Owner: `context-engineer`.

2. **[I2] Update `docs/architecture.md` skill count and agent listing** — bump "35 skills (2026-04-16)" to "49 skills (2026-05-12)", add new 4 skills to key-files list, add `interface-designer` to Agents row. Can be done in the merge commit or immediately post-merge. Owner: `doc-engineer`.

3. **[I3] Fix broken `scripts/git-post-merge-hook.sh` references in `docs/architecture.md`** — pre-existing; replace 4 occurrences with `scripts/git-finalize-hook.sh`. Owner: `doc-engineer`.

4. **[I5] Add staleness markers for `api-design-craft` and `agentic-interface-design` reference file sub-sections** — 5 markers total; removes F07 cold-start WARNs on next pass. Owner: `context-engineer`.

5. **[S1] Normalize `tui-design` staleness frontmatter format** — change embedded-date section names to bare section names + add in-file markers. Owner: `context-engineer`.

6. **[I4] Install git hooks in worktree** — run `bash install.sh` to install finalize + pre-commit hooks before merge. Owner: `user`.

7. **[S6] Resolve td-021 in TECH_DEBT_LEDGER** — verify resolution steps complete, migrate to RESOLVED. Owner: `doc-engineer`.

8. **[S7] Add bidirectional collaboration note in verifier → interface-designer** — minor coherence improvement. Owner: `context-engineer`.
