# Sentinel Report

**Run date:** 2026-05-01
**Compared to:** SENTINEL_REPORT_2026-04-17_19-25-00.md (grade A, 0/0/3)
**Commits since last run:** ~40 (AaC/DaC v1 + v1.1, bidirectional REQ↔arch traceability, tech-debt ledger pair, token-budget pass, /co+/cop extraction, landscape watchlist, finalize_adrs.py fixes, LikeC4 skill)

---

## Ecosystem Health: B

### Summary

The ecosystem absorbed a large burst of architectural work (~40 commits, +25 ADRs to 114 total, new `architect-validator` agent, new `likec4-querying` skill, tech-debt ledger pair contract) and is in good shape. No Critical findings. The **Important** tier has two structural items: `architect-validator` is absent from `agents/README.md` (catalog drift) and 31 of 33 staleness-sensitive sections across 6 skills lack `<!-- last-verified: -->` markers (F07 cold-start — by-design on first pass after backfill). The **Suggested** tier surfaces 3 new hotspot entries from the 2026-04-28 metrics run and 1 new coverage gap. Token budget is at 93.9% of ceiling (conservative estimate) — still within range but tightening.

Compared to 2026-04-17 baseline (A, 0/0/3): grade degraded to B due to architect-validator catalog gap and F07 cold-start WARNs, both expected after a large growth burst.

---

## Ecosystem Coherence: B

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | WARN | `architect-validator` in coordination protocol Available Agents table but absent from `agents/README.md` Mermaid pipeline diagram and agent table |
| EC04 | PASS | All pipeline stages (promethean → researcher → architect → planner → implementer/test-engineer/doc-engineer → verifier) have producing and consuming agents; architect-validator wired as independent pre-merge gate |
| EC05 | PASS | AaC/DaC enforcement layer, bidirectional traceability, and LikeC4 querying skill cover the structural gaps previously noted in the roadmap; no obvious missing artifact types given current goals |
| EC07 | PASS | `scripts/check_aac_golden_rule.py --mode=audit --json` returns `[]` with exit 0; no golden-rule drift found |

### Cross-dimension anomalies

- **F07 cold-start across 6 skills**: `python-prj-mgmt`, `claude-ecosystem`, `deployment`, `llm-prompt-engineering`, `agentic-sdks`, `communicating-agents` all declared `staleness_sensitive_sections` but have not yet received `<!-- last-verified: -->` markers. This is the expected first-run state per the cold-start semantics in `rules/swe/staleness-policy.md`. `likec4-querying` correctly has markers (sections `## Decision Rubric` and `## MCP Tool Quick Reference` both verified 2026-04-30).

- **Catalog surface drift pattern**: The addition of `architect-validator` was registered in `plugin.json`, `swe-agent-coordination-protocol.md`, and `agent-intermediate-documents.md`, but `agents/README.md` table, Mermaid diagram, and plugin.json example block were not updated. Classic growth-burst catalog drift.

### Artifact coherence distribution

Skills: 38 × A (all checks PASS). Agents: 13 × A, 1 × B (`architect-validator` — agent itself is well-formed, but its catalog presence in README is incomplete). Rules: 9 × A. Commands: 30 × A. ADRs: 114 × A (all finalized, no lingering dec-draft references, DL03 index consistent).

### Synthesis

The system works well as a connected whole. The AaC/DaC enforcement layer is coherent end-to-end: `architect-validator` agent, `aac_fence_validator.py` script, `check_aac_golden_rule.py` script, `rules/writing/aac-dac-conventions.md`, `skills/likec4-querying`, and `scripts/finalize_adrs.py` all form a consistent unit. The tech-debt ledger pair (LEDGER.md + RESOLVED.md) contract is internally consistent across `rules/swe/agent-intermediate-documents.md` and the `architect-validator` agent prompt. The single friction point is the README catalog gap for `architect-validator`.

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 38 | +3 from last run (likec4-querying + 2 others) |
| Agents | 14 | +1 (architect-validator) |
| Rules | 9 unconditional + 3 path-scoped | stable |
| Commands | 30 | +2 (co, cop split canonical blocks) |
| Finalized ADRs | 114 | +66 since 2026-04-17 |
| Draft ADRs | 0 | all promoted |
| Reference files | 127 | +40 from last run |
| Always-loaded chars | 82,141 | ↑ from prior run |
| Always-loaded tokens (÷3.5 est.) | 23,469 / 25,000 | 6.1% headroom — TIGHTENING |
| Always-loaded tokens (÷4.0 est.) | 20,535 / 25,000 | 17.9% headroom |
| Active ledger rows | 5 | +0 pre-write (td-002..td-005, td-011) |
| Resolved ledger rows | 6 | td-001, td-006..td-010 |
| Coverage overall | 80.07% | stable |
| Hotspot rank-1 | otel_relay.py (58023) | stable (td-004 open) |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `skills/` (38) | Skills | A | A | B (F07: 31 cold-start WARNs across 6 skills) | A | A | A (all <400L) | A | B |
| `agents/` (14) | Agents | A | A | A | A | A | B (sentinel.md 559L FAIL; 5× WARN 300-500L) | B (arch-validator missing from README) | B |
| `rules/` | Rules | A | A | A | A | A | A | A | A |
| `commands/` (30) | Commands | A | A | A | A | A | A | A | A |
| ADRs (114) | Decisions | A | A | A | A | A | A | A | A |
| `plugin.json` | Config | A | A | A | A | A | A | A | A |
| `agents/README.md` | Catalog | A | A | A | A | B (arch-validator absent) | A | B | B |
| `.ai-state/ARCHITECTURE.md` | Architecture | A | A | A | A | A | A | A | A |

---

## Findings

### Critical (blocks correct behavior)

*None.*

---

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| I1 | X06/EC01 | Cross-Ref / Ecosystem Coherence | `agents/README.md` | `architect-validator` exists in `agents/architect-validator.md`, `plugin.json`, and the coordination-protocol Available Agents table, but is absent from the `agents/README.md` agent table, the Mermaid pipeline diagram, and the plugin.json code block example in the README. Agent catalog is 13/14 complete. | Add `architect-validator` row to the README table (boundary vs verifier/sentinel/doc-engineer columns); add a node to the Mermaid diagram; update plugin.json example block. | `context-engineer` |
| I2 | F07 | Freshness | `skills/python-prj-mgmt` (6 sections), `skills/claude-ecosystem` (9 sections), `skills/deployment` (1 section), `skills/agentic-sdks` (3 sections), `skills/communicating-agents` (9 sections) — total 28 sections | F07 cold-start: `staleness_sensitive_sections` declared in frontmatter but no `<!-- last-verified: YYYY-MM-DD -->` markers placed in the SKILL.md body sections. Per staleness policy, first sentinel run after backfill produces N WARNs; subsequent runs shrink the set as markers are added. `communicating-agents` has `staleness_threshold_days: 60` — tighter window once markers are placed. | Place `<!-- last-verified: 2026-05-01 -->` immediately after each section heading listed in `staleness_sensitive_sections`. | `context-engineer` |
| I3 | F07 | Freshness | `skills/llm-prompt-engineering` — `Gotchas` section | F07 cold-start: 2 of 3 staleness-sensitive sections have markers (`Model Family Matrix`, `Platform Comparison` verified 2026-05-01) but `Gotchas` is missing its marker. | Place `<!-- last-verified: 2026-05-01 -->` after `## Gotchas` heading. | `context-engineer` |

---

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | T03 | Tokens | `agents/sentinel.md` (559 lines) | Sentinel agent prompt exceeds T03 FAIL threshold (500 lines). The check catalog embedded in the sentinel's own prompt is the primary driver. Structural concern: the Check Catalog is ~350+ lines of the 559 total — it is payload, not logic, and is already organized for progressive disclosure if extracted. | Extract the Check Catalog section to a reference file (e.g., `skills/software-planning/references/sentinel-check-catalog.md` or a dedicated skill) and load on demand. This is a pre-existing condition; flagged again given the token headroom trend. | `context-engineer` |
| S2 | T02 | Tokens | Always-loaded surface (`rules/swe/agent-intermediate-documents.md` 21,295 chars, `rules/swe/swe-agent-coordination-protocol.md` 14,670 chars, `rules/swe/adr-conventions.md` 13,944 chars) | Always-loaded budget at 93.9% of 25,000-token ceiling (conservative ÷3.5 estimate): 82,141 chars total. The 6.1% headroom is declining run-over-run (18.4% → 10.6% → 5.2% → now 6.1% — slight recovery from token-budget pass, but three large unconditional rules remain). One more substantive unconditional addition will breach budget at ÷3.5 estimate. | Before adding any unconditional rule content, re-measure. Consider `paths:` scoping or skills for any content not needed in >30% of sessions. The RESERVED L2 cut from td-008 (TECH_DEBT_LEDGER schema extraction) remains an option if headroom becomes critical. | `context-engineer` |
| S3 | TD | Tech Debt | `hooks/send_event.py` | New TD01 finding: rank-8 hotspot (score=11,118), complexity=34 — the highest in the codebase after `otel_relay.py` (27). Always-executed pre-commit hook: impact amplified by execution on every commit. Row filed as `td-014`. | See `td-014` in `TECH_DEBT_LEDGER.md`. | `implementer` |
| S4 | TD | Tech Debt | `scripts/reconcile_ai_state.py` | New TD03 finding: coverage at 60.1%, below the 70% project floor. Infrastructure script handling `.ai-state/` reconciliation — logic errors in untested paths could corrupt persistent project state. Row filed as `td-015`. | See `td-015` in `TECH_DEBT_LEDGER.md`. | `test-engineer` |
| S5 | C01 | Completeness | `skills/` | C01 count discrepancy: 41 SKILL.md files vs 38 skill dirs. The 3 extras are test fixtures under `skills/skill-crafting/tests/fixtures/validate_references/skills/`. These are intentional and correct, but the automated check produces a count mismatch. | Pre-existing known false positive. No action needed, but consider excluding the `tests/` subtree from C01 counting logic. | `context-engineer` |
| S6 | TD | Tech Debt | `scripts/project_metrics/report.py`, `memory-mcp/src/memory_mcp/metrics.py` | New TD01 findings: rank-2 (score=21,399, complexity=21) and rank-4 (score=15,540, complexity=28) hotspots not yet in ledger. Rows filed as `td-012` and `td-013`. | See `td-012`, `td-013` in `TECH_DEBT_LEDGER.md`. | `implementer` |

---

## Pipeline Discipline

Task Chronograph MCP is available in the environment. However, no active pipeline was running at audit time — no `get_pipeline_status` query was executed as the audit itself is the active task. Prior calibration log entries (2026-04-30) show Standard and Lightweight tiers were both selected correctly and ran end-to-end within tier constraints. No P-dimension findings.

---

## Tech-Debt Findings

**New rows filed this run: 4** (td-012 through td-015)

| id | class | severity | source | notes |
|----|-------|----------|--------|-------|
| td-012 | complexity | suggested | sentinel | `scripts/project_metrics/report.py` rank-2 hotspot; complexity=21, churn=1019; actively maintained |
| td-013 | complexity | suggested | sentinel | `memory-mcp/src/memory_mcp/metrics.py` rank-4 hotspot; complexity=28 (2nd highest in codebase); active dev |
| td-014 | complexity | important | sentinel | `hooks/send_event.py` rank-8 hotspot; complexity=34 (highest after otel_relay); always-executed pre-commit hook |
| td-015 | coverage-gap | important | sentinel | `scripts/reconcile_ai_state.py` at 60.1% — below 70% floor; handles .ai-state/ reconciliation |

**LLM judgment rationale:**
- td-012: Warranted because `report.py` is rank-2 overall with high churn+complexity — it's the central reporting aggregator and likely to keep growing. Not yet at the 800-line ceiling but trending there.
- td-013: Warranted because complexity=28 makes this the second-highest-complexity module in the codebase (behind only `otel_relay.py`'s 27 in the hotspot list), and it's under active development in the memory-mcp subsystem.
- td-014: Warranted as `important` (not just `suggested`) because `send_event.py` has complexity=34 — the *highest* in the codebase — and runs on every pre-commit execution. High complexity in an always-executed path has outsized impact on reliability.
- td-015: Warranted as `important` because `reconcile_ai_state.py` handles `.ai-state/` mutation (the permanent project-intelligence store). Untested paths in this module carry higher risk than untested paths in, e.g., a reporting script.

**Skipped (test files, low complexity, or borderline):**
- `test_runner.py`, `test_git_collector.py`, `test_pydeps_collector.py`, `test_scc_collector.py`, `test_otel_relay.py` — test files; churn reflects active iteration not structural debt
- `scripts/render_claude_md.py` at 69.1% — borderline (one percentage point under floor); not warranted given low risk of the rendering path

**TD05 discipline check:** PASS — no in-flight rows older than 30 days, no unassigned rows older than 7 days, no terminal-status rows in the wrong file.

**Existing open rows (td-002..td-005, td-011):** All appropriate owner-roles, no discipline violations. td-011 (`finalize_adrs.py` defects) correctly tagged `implementer` at `suggested`.

---

## Special Focus Areas (per audit request)

### Tech-debt ledger pair contract

**PASS.** The `agent-intermediate-documents.md` "Writers (only four)" header correctly lists all four writers: verifier, sentinel, orchestrator, and architect-validator. The `architect-validator` agent prompt's `TECH_DEBT_LEDGER row schema` section is internally consistent with the rule's schema definition. The `TD05` status-update-discipline check found no violations. RESOLVED.md has 6 rows, all with `resolved-by` populated where `status=resolved`.

**One minor inconsistency detected:** The rule states "Writers (only four)" but the header title could cause confusion since architect-validator is listed as the 4th writer, not in the introduction sentence that reads "canonical sources are verifier and sentinel." This sentence refers to the primary producers for routine cases — the wording is correct but may be misread. No action required.

### Token budget (re-measured)

**82,141 chars** total always-loaded surface (3 CLAUDE.md files + 7 unconditional rules). At ÷3.5 conservative: **23,469 tokens (93.9%)** — headroom 6.1%. At ÷4.0 realistic: **20,535 tokens (82.1%)** — headroom 17.9%. The token-budget pass (td-007..td-010) achieved ~1,550 tokens of headroom recovery. The budget has stabilized but not improved: the addition of `adr-conventions.md` content and `aac-dac-conventions.md` are net new unconditional rule content. Monitoring recommended before any further unconditional additions.

### ADR finalize cross-reference integrity

**PASS.** All 114 finalized ADRs have correct `dec-NNN` frontmatter. No lingering `dec-draft-<hash>` values in frontmatter cross-reference fields. The two mentions of `dec-draft-` in finalized ADRs (056 and 061) are in protocol-explanation prose describing the scheme, not in live cross-reference fields — correctly not flagged. DL03 index count matches file count exactly (114/114).

### AaC/DaC + architect-validator integration

**MOSTLY PASS, one gap.** The `architect-validator` agent prompt is well-formed and coherent: boundary table is precise, process phases are numbered, tech-debt ledger writer role is correctly defined. It is registered in `plugin.json` and the coordination protocol. **Gap:** `agents/README.md` table and Mermaid diagram omit architect-validator entirely — see finding I1.

### Path-scoped skill frontmatter

**PASS.** Only `skills/likec4-querying/SKILL.md` has `paths:` frontmatter, and it is correctly formatted as a YAML list (`['**/*.c4', '**/ARCHITECTURE.md', 'docs/architecture.md', 'docs/diagrams/**']`), not a string. No other skills have malformed path-scoped frontmatter.

---

## Recommended Actions (prioritized)

1. **[I1] Add `architect-validator` to `agents/README.md`** — table row + Mermaid diagram node + plugin.json code block example. Quick doc fix; no logic changes. Owner: `context-engineer` or `doc-engineer`.

2. **[I2/I3] Add `<!-- last-verified: -->` markers to 31 staleness-sensitive sections** across `python-prj-mgmt`, `claude-ecosystem`, `deployment`, `agentic-sdks`, `communicating-agents`, and `llm-prompt-engineering`. Cold-start — place today's date. Owner: `context-engineer`.

3. **[S1] Extract sentinel check catalog** to a reference file to bring `agents/sentinel.md` under the 500-line T03 threshold. Pre-existing; deprioritize unless token headroom reaches critical. Owner: `context-engineer`.

4. **[S2] Monitor token budget** — 6.1% headroom at conservative estimate. No new unconditional rule content without re-measuring first. Owner: user decision gate.

5. **[S3/S4/S6] Review new TD rows (td-012..td-015)** — td-014 (`hooks/send_event.py` complexity) and td-015 (`reconcile_ai_state.py` coverage) are `important`; schedule accordingly. Owner: `implementer` / `test-engineer`.
