# Sentinel Report

**Run:** 2026-05-17 13:05:50
**Prior baseline:** 2026-05-13 05:46:56 (Health A / Coherence A, 0/0/3 findings)
**Branch at scan:** `feat/new-project-hackathon-flag`
**Scope:** Full exhaustive sweep — all dimensions, all artifact classes

---

## Ecosystem Health: B

### Summary

The ecosystem remains in strong shape — 49 skills, 15 agents, 190 ADRs, 39 commands, 20 rules, 190 ADRs. The 2026-05-13 doc-arch-sync wave cleared all prior Important findings and three Suggested items resolved cleanly. Since that baseline, seven commits landed (`feat/new-project-hackathon-flag` branch through `e3f51a1`) adding the hackathon mode feature, dec-189/dec-190 ADRs, a metrics report, and a greenfield worked example — none introduced new structural failures.

**What improved:** S1 and S3 carry-forwards remain consistent with prior audit (see verdicts below). The skills/README.md catalog is exactly in sync with filesystem (49 skills, 49 entries). The rules/README.md uses a file-tree format that exactly matches the filesystem. The DL03 ADR index (190 entries, 190 files) is perfectly aligned. EC07 AaC golden-rule script reports zero important findings.

**What degraded:** One new Important finding (I1): the `commands/README.md` is missing `dispatch-reworks` — a command added in the rework-loop pipeline wave that was not reflected in the catalog. Two Suggested carry-forwards from 2026-05-13 remain open (S2: stale path in SPEC_multi-language-support; S3: sentinel.md and verifier.md exceed the 500-line FAIL threshold). One new Suggested (S5): README_DEV.md is missing `architect-validator.md` and `interface-designer.md` from the agents tree. Token budget has decreased slightly: 23,434 tokens (÷3.5), 6.3% headroom (was 10.3% last run) — still within the 25,000-token ceiling.

**Comparison to last run:** Health A → B (one new Important finding). Coherence A stable (no system-level EC FAILs). Finding counts: 0/0/3 → 0/1/5.

---

## Ecosystem Coherence: A

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | All 15 agents in coordination protocol table match `agents/*.md` 1:1 |
| EC04 | PASS | Agent pipeline has complete handoff coverage: promethean → researcher → systems-architect → implementation-planner → implementer/test-engineer → verifier; all outputs have producing and consuming agents |
| EC05 | PASS | No structural gaps for stated purpose; hackathon mode now covered by both CLAUDE.md and README.md |
| EC06 | PASS | Condensed deliverables block in `~/.claude/CLAUDE.md` lines 48-51 matches `skills/software-planning/references/coordination-details.md § Delegation Checklists` — all four agents aligned including conditional clauses (if deployment, if structural, if tests) |
| EC07 | PASS | `scripts/check_aac_golden_rule.py --mode=audit --json` returns `[]` — zero important-severity findings |

### Cross-dimension anomalies

- Token budget headroom has declined from 10.3% → 6.3% over the last four days. This is a structural drift trend: each new always-loaded rule or CLAUDE.md expansion costs headroom permanently. The current 6.3% is within safety range but declining.
- `dispatch-reworks` command was added without a corresponding README update — consistent with the growth-burst drift pattern documented in agent memory.

### Artifact coherence distribution

Scorecard coherence column: 48 A, 1 B (commands/dispatch-reworks missing from catalog), 1 C (SPEC_multi-language-support stale path). All agent, skill, and rule coherence grades are A.

### Synthesis

The ecosystem functions as a coherent whole. The pipeline is fully connected, ADR cross-references resolve, behavioral contract is consistently applied across all 11 code-emitting agents, and the LikeC4 fence validator passes on `docs/architecture.md`. The one Important finding is a catalog maintenance gap, not a structural defect. The system-level architecture is stable.

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 49 | Stable (was 49 on 2026-05-13) |
| Agents | 15 | Stable |
| Rules (unconditional) | 8 | Stable |
| Rules (path-scoped) | 13 | Stable |
| Commands | 39 | +4 since 2026-05-13 (resume-rework, dispatch-reworks, skill-genesis, skill-genesis-review) |
| ADRs (finalized) | 190 | +26 since 2026-05-13 |
| ADRs (drafts) | 0 | Stable |
| Reference files | 166 | Stable |
| Active TD rows | 15 | Stable (was 12 active after 2026-05-13 cleanup; 3 new: td-033/td-034/td-035 filed post-cleanup) |
| Always-loaded tokens (÷3.5) | 23,434 | Declining (was 22,436 on 2026-05-13, 6.3% headroom) |
| Coverage line % | 0.83% | Stable (was 0.84% 2026-05-13) |
| Top hotspot score | 110,532 | Stable |
| F07 WARN count | 40 | Stable (cold-start semantics; expected on each pass until markers are added) |
| Calibration log rows | 4 | Stable (CA02 requires 5+; skip pending next row) |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| skills/* (49) | Skills | A | A | A (F07 WARNs cold-start expected) | A | A | A | A | **A** |
| agents/* (15) | Agents | A | A | A | B (sentinel 567L, verifier 577L > FAIL threshold) | A | A | A | **B** |
| rules/* (21) | Rules | A | A | A | A | A | A | A | **A** |
| commands/* (39) | Commands | A | A | A | A | B (dispatch-reworks missing from README) | A | B | **B** |
| CLAUDE.md | Config | A | A | A | A | A | A | A | **A** |
| plugin.json | Config | A | A | A | A | A | A | A | **A** |
| ADRs (190) | Decisions | A | A | A | A | A | A | A | **A** |
| Specs (5) | Specs | B (1 stale path) | A | B | A | B | A | B | **B** |
| DESIGN.md | Architecture | A | A | A | A | A | A | A | **A** |
| docs/architecture.md | Architecture | A | A | A | A | A | A | A | **A** |
| TECH_DEBT_LEDGER.md | Debt | A | A | A | A | A | A | A | **A** |
| README_DEV.md | Doc | B (2 agents missing) | A | A | A | B | A | B | **B** |

---

## Findings

### Critical (blocks correct behavior)

None.

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| I1 | X02/EC02 | Cross-Reference / Ecosystem | `commands/README.md` | `commands/dispatch-reworks.md` exists on disk but is not listed in `commands/README.md`. The command was added in the rework-loop pipeline wave (`cd13ffc`). FS: 39 commands; README: 38 entries. | Add a row for `/dispatch-reworks` to the appropriate section in `commands/README.md`. | `context-engineer` |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | F03 | Freshness | `.ai-state/DESIGN.md:244` | The "Git is the only shared synchronization substrate" constraints row contains the phrase "see draft ADRs under `.ai-state/decisions/drafts/` (promoted to `dec-NNN` at merge-to-main)". This is a generic process description, not a stale slug — but when `drafts/` is empty (0 drafts currently), the phrase "draft ADRs under drafts/" could mislead readers into thinking there are live drafts to consult. Consider replacing with a reference to the finalized ADRs that embody this decision (e.g., dec-056). **Severity: Suggested only** — no functional impact. | Rewrite the constraints row to cite the finalized ADR(s) rather than pointing to an empty drafts/ directory. | `context-engineer` |
| S2 | SH01 | Spec Health | `.ai-state/specs/SPEC_multi-language-support_2026-05-11.md` | References `skills/mcp-crafting/references/python-resources.md` (line ~92) which was migrated to `skills/mcp-crafting/contexts/python.md` as part of the pipeline that produced this spec. The spec is archived (post-feature); the stale path does not break anything but violates the traceability contract. | Update the stale path in the spec's affected-files or traceability section. Alternatively, document the migration as a note in the spec. **Carry-forward from 2026-05-13.** | `context-engineer` |
| S3 | T03/S05 | Token / Spec | `agents/sentinel.md` (567 lines), `agents/verifier.md` (577 lines) | Both exceed the 500-line FAIL threshold for agent prompts (warn at 300, fail at 500). `implementation-planner.md` at 461L and `systems-architect.md` at 401L are WARN. The sentinel and verifier encapsulate complex multi-phase protocols that are difficult to trim without losing precision. However, the size is a token tax on every session that activates them. **Carry-forward from 2026-05-13.** | Evaluate whether check catalogs, example tables, or protocol details can be extracted to reference files. Even extracting 50-100 lines from each would shift them from FAIL to WARN. | `context-engineer` |
| S4 | F07/Freshness | Freshness | 15 skills with `staleness_sensitive_sections:` | 40 sections across 15 skills have `staleness_sensitive_sections:` declared in frontmatter but no `<!-- last-verified: -->` marker following the section heading. This is **expected cold-start behavior** — skills recently backfilled the frontmatter key but have not yet had their first marker-writing pass. The F07 WARN set will shrink as markers are added. Skills affected: `agentic-interface-design`, `agentic-sdks`, `api-design-craft`, `claude-ecosystem`, `communicating-agents`, `deployment`, `experiment-tracking`, `likec4-querying`, `llm-prompt-engineering`, `llm-training-eval`, `ml-training`, `neo-cloud-abstraction`, `python-prj-mgmt`, `tui-design`, `web-ui-design`. | Run a staleness marker sweep: for each skill's sensitive sections, add `<!-- last-verified: 2026-05-17 -->` after the section heading if content is current; add an older date if the section is known stale. | `context-engineer` |
| S5 | F01/X06 | Freshness / Cross-Ref | `README_DEV.md:73-88` (agents tree) | `README_DEV.md` agents directory listing omits `architect-validator.md` and `interface-designer.md`. Both agents exist in `agents/` and are registered in `agents/README.md`, `plugin.json`, and the coordination protocol. The tree was last updated when the 15-agent roster was at 13 agents (before architect-validator and interface-designer were added). | Add the two missing agent entries to the README_DEV.md agents tree section. | `context-engineer` |

---

## Pipeline Discipline

Skipped: Task Chronograph MCP tools (`get_pipeline_status`, `get_agent_events`) not queried in this run. The `.ai-state/sentinel_reports/SENTINEL_LOG.md` and recent metrics reports were used as proxy. No active pipelines detected (branch `feat/new-project-hackathon-flag` is a post-merge implementation branch, no `.ai-work/` active tasks observed).

---

## Tech-Debt Findings

### Summary

No new TD rows filed this run. The `METRICS_REPORT_2026-05-15_22-58-55.md` (2 days old, well within the 14-day staleness threshold) was read for TD01/TD02/TD03/TD04 analysis.

**TD01 (hotspots):** The top-10 hotspot list shows `dashboard_app/src/components/metrics-dashboard.tsx` (score 110,532: churn 1,812 × CCN 61, 448 lines) as the top hotspot. This is a significant concentration of complexity and churn. However, `metrics-dashboard.tsx` at 448 lines is under the 800-line ceiling — the complexity is dense but the file is not oversized. A new ledger row is not warranted at this score level given that: (a) the file is under the line ceiling, (b) no existing td-NNN covers it, (c) the dashboard component is a legitimate high-churn surface during active development. If churn continues at the next metrics run, re-evaluate. No new row filed.

**TD02 (cyclic deps):** `cyclic_deps: 0` in the latest metrics report. PASS — no new rows.

**TD03 (coverage gaps):** `coverage_line_pct: 0.83%` project-wide. This is extremely low but has been a known condition throughout the project's history (primarily Markdown/knowledge artifacts make up the codebase, skewing line count vs. testable Python). No change from prior run. Coverage signal is not degrading. No new rows.

**TD04 (p95 complexity):** `ccn_p95: 8`, `cognitive_p95: 9`. Both are well within acceptable bounds for a Python codebase. `otel_relay.py` (td-004) remains at 1,468 lines with CCN 27 (rank-6 hotspot). No new p95 crossings beyond already-tracked rows.

**TD05 discipline findings:**
- `td-033` and `td-035` have `owner-role: unassigned`. As of 2026-05-17 (audit date), `td-033` is 5 days old and `td-035` is 3 days old — both under the 7-day TD05(c) threshold. No TD05 WARN triggered.
- No `status: in-flight` rows exist in the active ledger.
- No `status: resolved` rows in RESOLVED.md are missing `resolved-by` (spot-checked the resolved file header format).

### Active Ledger Row Re-Validation

**Purpose:** Ground-truth verification requested by the audit consumer. Each row was checked against its cited location on disk as of 2026-05-17.

| id | Still grounded? | Evidence | Recommended action |
|----|----------------|----------|--------------------|
| td-002 | **YES** | `claude/canonical-blocks/agent-pipeline.md` is 1,921 bytes / ~549 tokens. Still ~2x the ~250-token per-block budget. `hooks/test_onboard_praxion_block.py::test_block_stays_within_token_budget[agent-pipeline]` is xfailed. | Hold open. Needs content review + ADR before trimming changes injection contract. |
| td-003 | **YES** | `memory-mcp/src/memory_mcp/store.py` is exactly 947 lines — still at the cited count, over the 800-line ceiling in `rules/swe/coding-style.md`. | Hold open. Module split (auto-link logic, helper functions, incoming-link scanning) still required. `implementation-planner` owner is correct per restructuring heuristic. |
| td-004 | **YES** | `task-chronograph-mcp/src/task_chronograph_mcp/otel_relay.py` is exactly 1,468 lines — unchanged, 1.84x the 800-line ceiling. Rank-6 hotspot in latest metrics (score 18,036). | Hold open. Module split still required. Worsening is arrested (count unchanged from prior audit) but the file remains the largest in the codebase by a significant margin. |
| td-005 | **YES** | `eval/pyproject.toml` lists `arize-phoenix-evals>=3.0` but NOT `arize-phoenix`. `eval/src/praxion_evals/regression/` directory exists. The architectural flaw (task_slug keying collapses all Praxion pipelines; missing `arize-phoenix` dep; swallowed `AttributeError`) is not observable from the file system without running the eval — the cited files still exist and the dep gap is confirmed. | Hold open. `implementation-planner` ownership correct (requires ADR + schema change before code). |
| td-012 | **YES** | `scripts/project_metrics/report.py` is 929 lines — over the 800-line ceiling. Still rank-3 hotspot (score 21,399, churn 1,019 × CCN 21) in latest metrics report. Trend is stable (unchanged score). | Hold open. Module split (`report_sections/` package) still warranted. `implementation-planner` ownership correct. |
| td-013 | **YES** | `memory-mcp/src/memory_mcp/metrics.py` is 503 lines, well under 800-line ceiling. Still high CCN density but dropped out of top-10 hotspot list. Last notes on 2026-05-13 correctly de-escalated to `suggested`. | Hold open at `suggested`. Close if team accepts current density. The file is not in the top-10 hotspot list as of the 2026-05-15 metrics report. **Consider retiring to RESOLVED as `wontfix`** if churn stays low. |
| td-021 | **YES** | `CLAUDE.md` contains the `Explore` workaround bullet (line 114). The upstream issue `#57493` is referenced. The root cause (shipped Explore crashes in many-skill sessions) has not been patched upstream. | Hold open. The workaround is documented. Owner is `doc-engineer` (documentation+workaround, not a code fix). |
| td-024 | **YES** | `dashboard_app/tests/` contains only `server/` and `support/` directories — no jsdom/`@testing-library/react` tests exist yet. The missing React component render tests are confirmed absent. | Hold open. `test-engineer` ownership correct. |
| td-025 | **YES** | `dashboard_app/src/components/shells/` contains only `default.tsx`, `explanation.tsx`, `reference.tsx`, and `index.ts` — `tutorial`, `how-to`, and `concepts` shell components are absent. | Hold open. `implementation-planner` ownership correct. |
| td-027 | **YES** | `dashboard_app/src/components/registry.ts` exists. The problem is not file absence but missing renderer registrations inside the file (metrics_view, plan_view, verification_report, idea_grid, architecture_explorer). Not verified at the line level (out of scope for this audit pass), but the framing matches the v1-only-ships-three-Diataxis-shells context. | Hold open. `implementation-planner` ownership correct. **Note:** if any of the five cited renderer types have been added since 2026-05-11, this row may be partially resolved. A consumer should grep `registry.ts` for those renderer names before acting. |
| td-029 | **YES** | `dashboard_app/src/server/view-models/documentation.ts` exists. The `assertContainedProjectPath` wide-allowlist security issue is cited in the file. Not line-verified in this pass, but the framing (security hardening deferred, class `other`, owner `systems-architect`) is consistent with the dashboard v1 scope. | Hold open. `systems-architect` ownership correct for security-boundary decisions. |
| td-033 | **YES — but recommend resolving as `wontfix`** | The path-scoped rules read-only trigger is confirmed upstream-declined (`#23478` and `#38487` both closed `NOT_PLANNED`). `scripts/check_paths_syntax.py` reports all 13 path-scoped rules use correct `paths:` syntax. The agent-prompt mitigation is in place (`implementer.md`, `doc-engineer.md`, `test-engineer.md` all have "read a sibling first" instructions). The note in `skills/rule-crafting/SKILL.md` exists. The condition is grounded as a **permanent design constraint**, not actionable tech debt. `owner-role: unassigned` is correct because there is no implementation action available. | **Recommend: migrate to `TECH_DEBT_RESOLVED.md` as `wontfix`** with `resolved-by: upstream-declined`. This is the cleanest ledger hygiene — the row has served its purpose (trigger → documentation → permanent mitigation), and leaving it `open` with `unassigned` owner creates noise. |
| td-034 | **YES** | `hooks/worktree_guard.py` and `hooks/inject_worktree_banner.py` exist. The subagent cwd resolution problem (writes landing in canonical checkout instead of worktree) was observed 2026-05-13 and is unresolved. The three root-cause hypotheses remain unvalidated. No controlled-test script has been written. | Hold open. `implementation-planner` ownership correct. Resolution requires a hook-scope architecture decision. |
| td-035 | **YES — but consider resolving as `wontfix`** | `CLAUDE.md` has the sandbox policy limitation documented (line 116). The root cause (per-tool permission prompts not propagating to subagents) is an upstream-only fix. The workaround (main agent proxies the write) is documented. Similar to td-033, this is a permanent upstream constraint with a documented workaround. `owner-role: unassigned` is correct. | **Recommend: evaluate for `wontfix` alongside td-033** — same upstream-gated pattern. If the permanent workaround is accepted, migrate to RESOLVED with a note pointing to CLAUDE.md Known Claude Code Limitations. |
| td-036 | **YES** | `install.sh` has no `--dev-link` or `--editable` flag (confirmed: `grep -n "dev-link"` returns nothing). The partial resolution note (manual `cp` workaround documented in `README_DEV.md`) is confirmed at line 529 of README_DEV.md. The underlying `--dev-link` mode is still pending. | Hold open. `implementation-planner` ownership correct. |

---

## Self-Verification (V)

| Check | Result | Evidence |
|-------|--------|----------|
| V01 | PASS | `./agents/sentinel.md` in `plugin.json` agents array |
| V02 | PASS | `sentinel` row present in `rules/swe/swe-agent-coordination-protocol.md` Available Agents table (3 matches) |
| V03 | PASS | `sentinel` row present in `agents/README.md` table (7 matches) |
| V04 | PASS | `## Check Catalog` section present in `agents/sentinel.md` (2 matches) |

---

## Spec Health (SH)

| Check | Result | Evidence |
|-------|--------|----------|
| SH01 | WARN | `SPEC_multi-language-support_2026-05-11.md` references `skills/mcp-crafting/references/python-resources.md` (absent; migrated to `contexts/python.md`). All other 4 specs PASS. |
| SH02 | PASS | All 5 specs have `## Traceability` or `REQ-` sections. |
| SH03 | PASS (LLM) | Spec behavioral claims are consistent with current skill content (spot-checked metrics spec vs `scripts/project_metrics/report.py` — collector outputs match spec REQs). |
| SH04 | PASS (LLM) | No UNTESTED requirement IDs visible in traceability matrices (multi-language-support spec has all REQs covered). |
| SH05 | PASS (LLM) | Key Decisions sections include what/why/alternatives in sampled specs. |
| SH06 | N/A | No spec has a predecessor archived spec to compare against. |

---

## Calibration Accuracy (CA)

| Check | Result | Evidence |
|-------|--------|----------|
| CA01 | PASS | `.ai-state/calibration_log.md` exists, header row present. |
| CA02 | SKIP | Only 4 data rows; CA02 requires 5+. |

---

## Decision Log (DL)

| Check | Result | Evidence |
|-------|--------|----------|
| DL01 | PASS | 190 finalized ADRs in `.ai-state/decisions/[0-9]*.md`; 0 draft fragments. |
| DL02 | PASS | Sampled dec-188, dec-189, dec-190 — all have required frontmatter fields (`id`, `title`, `status`, `category`, `date`, `summary`, `tags`, `made_by`). |
| DL03 | PASS | `DECISIONS_INDEX.md` has 190 `dec-` rows; filesystem has 190 finalized ADR files. Perfect alignment. |
| DL04 | PASS | Cross-reference spot-check on latest 10 ADRs: all `re_affirms`, `supersedes` pointers resolve to existing finalized files. |
| DL05 | PASS | Hackathon mode feature has dec-189 and dec-190. Test-baseline-disposition feature has dec-188. ADR frequency is healthy. |

---

## Architecture Completeness (AC)

| Check | Result | Evidence |
|-------|--------|----------|
| AC01 | PASS | `.ai-state/DESIGN.md` exists; project has 15+ interacting components. |
| AC02 | PASS (LLM) | Component names in `DESIGN.md` §3 are internally consistent. Every component in the Data Flow section appears in the Components table. Abstract names (Skills, Agents, Rules, Commands) are used consistently. |
| AC03 | PASS | 3/30 (10%) of sampled file paths in DESIGN.md do not resolve — `src/components/registry.ts`, `contexts/vue.md`, `contexts/typescript.md`. These are illustrative paths in skill/component descriptions referencing files that live under skill-specific `contexts/` dirs with different path prefixes. Under the 50% threshold. |
| AC04 | PASS | All `dec-NNN` references in `DESIGN.md` and `docs/architecture.md` resolve to existing finalized ADR files. |
| AC05 | PASS | `docs/architecture.md` exists. |
| AC06 | PASS (LLM) | Component names in `docs/architecture.md` §3 match actual module directories and file locations. |
| AC07 | PASS | File paths in `docs/architecture.md` component table resolve (spot-checked). |
| AC08 | PASS (LLM) | `docs/architecture.md` has no Status column and no Planned items — only Built components. |
| AC09 | PASS (LLM) | Every component in `docs/architecture.md` also appears in `.ai-state/DESIGN.md`. |
| AC10 | PASS | `python3 scripts/aac_fence_validator.py docs/architecture.md` → `Verdict: PASS` |
| AC11 | PASS (LLM) | LikeC4 model substrate present (`docs/diagrams/architecture/src/architecture.c4`); markdown substrate present. Component names in architecture.c4 are consistent with `docs/architecture.md`. |
| AC12 | SKIP | No `architectural_elements:` frontmatter in specs and no `metadata.req_ids` in LikeC4 elements detected — bidirectional convention not yet populated. |

---

## Behavioral Contract (BC)

| Check | Result | Evidence |
|-------|--------|----------|
| BC01 | PASS | `rules/swe/agent-behavioral-contract.md` exists; `grep -c "^paths:"` returns 0 (no paths: frontmatter). |
| BC02 | PASS | Global `~/.claude/CLAUDE.md` lines 63-66 list the four behaviors in correct canonical order: Surface Assumptions → Register Objection → Stay Surgical → Simplicity First. Project `CLAUDE.md` line 95 references all four in correct order within the Behavioral Contract section. |
| BC03 | PASS | All 11 code-emitting agents reference `rules/swe/agent-behavioral-contract.md`: researcher, systems-architect, implementation-planner, context-engineer, implementer, test-engineer, verifier, doc-engineer, sentinel, cicd-engineer, interface-designer. Count: 11/11 (plus architect-validator and others = 12 total). |
| BC04 | PASS | `skills/code-review/references/report-template.md` contains `### Behavioral Contract Findings` with all 6 canonical tags: `[UNSURFACED-ASSUMPTION]`, `[MISSING-OBJECTION]`, `[NON-SURGICAL]`, `[SCOPE-CREEP]`, `[BLOAT]`, `[DEAD-CODE-UNREMOVED]`. |

---

## Hackathon Mode (HK)

HK01 check: `PRAXION_HACKATHON_MODE=0` in `.claude/settings.json`. All HK checks skipped — hackathon mode is inactive for the Praxion meta-project itself. (The feature is implemented for user-managed projects, not for Praxion's own development workflow.)

---

## Test Topology (TT)

`.ai-state/TEST_TOPOLOGY.md` not present; TT checks skipped (opt-in protocol per `skills/testing-strategy/references/test-topology.md`). This is a normal state — no WARN or FAIL.

---

## Completeness and Consistency Summary (C/N)

| Check | Result | Evidence |
|-------|--------|----------|
| C01 | PASS | All 49 skill dirs have `SKILL.md` |
| C02 | PASS | All 49 SKILL.md files have `description:` in frontmatter |
| C03 | PASS | All 15 agent .md files have `name:`, `description:`, `tools:` |
| C04 | PASS (sampled) | Commands sampled have description fields |
| C05 | PASS | plugin.json: 15 agents = FS: 15 agents |
| C06 | PASS (LLM) | Skill descriptions are specific and activation-enabling; sampled skills clearly describe when to invoke |
| C07 | PASS (LLM) | Agent descriptions differentiate cleanly (promethean=ideation, sentinel=audit, verifier=review — no overlap confusion) |
| C08 | PASS | `[CUSTOMIZE]` sections in rules are intentionally unfilled templates; no filled-in sections left blank |
| C09 | PASS | `compose.yaml` / `Dockerfile` not present (no deployment configs); F09 skipped |
| N01 | PASS | All 49 skill dirs use lowercase kebab-case |
| N02 | PASS | All 15 agent files use lowercase kebab-case `.md` |
| N03 | PASS | Agent frontmatter keys are valid (`maxTurns`, `permissionMode`, `color`, etc. are valid per agent-crafting spec) |
| N04 | PASS (LLM) | Terminology is consistent: "context artifact" (not "context file"), "task slug" (not "slug"), "Always-loaded" consistent |
| N05 | PASS (LLM) | No contradictions between rules and agent prompts detected in sampled review |
| N06 | PASS (LLM) | Description style is consistent across agent table, skill descriptions, and command descriptions |

---

## Freshness Summary (F)

| Check | Result | Evidence |
|-------|--------|----------|
| F01 | WARN | `SPEC_multi-language-support_2026-05-11.md` references `skills/mcp-crafting/references/python-resources.md` (absent). All other F01 checks pass. |
| F02 | PASS | Spot-checked skill reference dirs; all paths resolve. |
| F03 | PASS (LLM) | Content references current tools/patterns. No replaced APIs or patterns detected. |
| F04 | PASS (LLM) | Agent prompts reference current pipeline stages and correct agent names. |
| F05 | PASS | `.ai-state/SYSTEM_DEPLOYMENT.md` referenced paths checked. |
| F06 | PASS (LLM) | Deployment doc service list is consistent (Next.js dashboard) with existing config files. |
| F07 | WARN (40 sections) | Cold-start semantics: 40 sections across 15 skills have `staleness_sensitive_sections:` but no `<!-- last-verified: -->` marker. Expected behavior per policy. |
| F08 | PASS | No sections have markers — F08 age check only applies to sections with existing markers. No failures. |
| F09 | PASS | No invalid or future-dated markers found (no markers exist yet). |
| F10 | WARN | `diff -q scripts/git-pre-commit-hook.sh .git/hooks/pre-commit` reports DIFF. The installed `pre-commit` hook is missing the new "Block B — Canonical-block sync" section (lines 161-192 in the source script). The installed copy lacks the `sync_canonical_blocks.py --check` enforcement added in the shipped-artifact-isolation wave. Run `install_claude.sh` to reinstall hooks. Note: `git-finalize-hook.sh` → `post-merge`/`post-commit` diffs are CLEAN. |

---

## Token / Spec Summary (T/S)

| Check | Result | Evidence |
|-------|--------|----------|
| T01 | PASS | No SKILL.md exceeds 500 lines. Largest: `context-security-review` at 396 lines. WARN(>400): none. |
| T02 | WARN | Always-loaded surface: 82,019 chars ÷ 3.5 = 23,434 tokens. Headroom: 6.3% (1,566 tokens). Declining from 10.3% on 2026-05-13. Budget ceiling: 25,000 tokens. Still within bounds but trend is negative. |
| T03 | FAIL | `sentinel.md` (567L), `verifier.md` (577L) exceed 500-line FAIL threshold. WARN: `implementation-planner.md` (461L), `systems-architect.md` (401L), `skill-genesis.md` (336L), `context-engineer.md` (358L), `promethean.md` (322L). |
| T04 | WARN | `skills/cicd/references/patterns-and-examples.md` (732 lines), `skills/python-prj-mgmt/references/pixi.md` (616 lines), `skills/python-prj-mgmt/references/uv.md` (565 lines), `skills/cicd/references/github-actions.md` (506 lines) exceed 500 lines. All are under the 800-line FAIL threshold. |
| T05 | PASS (LLM) | Progressive disclosure is used appropriately. Large agents (sentinel, verifier) have complex check catalogs that are load-bearing in their system prompt context — progressive disclosure to references is not feasible for operational check catalogs. |
| T06 | PASS (LLM) | No significant redundancy across artifacts detected. |
| S01 | PASS | All SKILL.md files have `description:` field (C02 confirms). |
| S02 | PASS | All 15 agent files have required frontmatter fields. |
| S03 | PASS | All rule files have `## ` level-2 heading after YAML frontmatter (`git-conventions.md` is the only one with a heading that appears directly after frontmatter — all others also pass after the frontmatter block ends). |
| S04 | PASS (sampled) | Commands using argument substitution have `$ARGUMENTS`. |
| S05 | PASS (LLM) | Skills follow progressive disclosure. No monolithic skill >400 lines without references. |
| S06 | PASS (LLM) | Agent prompts use numbered phases with clear completion criteria (spot-checked implementer, verifier, sentinel). |
| S07 | PASS (LLM) | Rules contain domain knowledge and constraints, not procedures. |

---

## Code Health (CH)

| Check | Result | Evidence |
|-------|--------|----------|
| CH01 | PASS (LLM) | Sampled `hooks/inject_rules.py` (rank-4 hotspot, 31 CCN), `scripts/finalize_adrs.py` (rank-5 hotspot, 20 CCN), and `scripts/project_metrics/report.py` (rank-3 hotspot, 21 CCN). No significant structural similarity or repeated logic blocks across these modules — each is a purpose-built standalone script. Shared patterns are appropriate (argparse usage, YAML frontmatter parsing). No cross-module duplication warranting a new row. |

---

## Recommended Actions (prioritized)

1. **[I1] Add `dispatch-reworks` to `commands/README.md`** (context-engineer) — simple catalog update, blocks the catalog from being stale for other consumers.

2. **[F10] Reinstall git hooks** (user) — run `bash install_claude.sh` to sync the installed `pre-commit` hook with the canonical source script. The Block B canonical-sync check is missing from the installed copy.

3. **[S3] Reduce sentinel.md and verifier.md below 500 lines** (context-engineer) — both exceed the agent-prompt FAIL threshold at 567 and 577 lines respectively. Extract check catalogs or supporting tables to reference files.

4. **[TD consolidation] Migrate td-033 and td-035 to `TECH_DEBT_RESOLVED.md` as `wontfix`** (user) — both are permanent upstream constraints with documented workarounds. Cleaning them from the active ledger removes noise for implementation consumers.

5. **[S4] Staleness marker sweep for 40 sections across 15 skills** (context-engineer) — cold-start F07 WARNs will persist until sections get their first `<!-- last-verified: -->` markers.

6. **[S2] Fix stale path in `SPEC_multi-language-support_2026-05-11.md`** (context-engineer) — update `skills/mcp-crafting/references/python-resources.md` reference to `skills/mcp-crafting/contexts/python.md`.

7. **[S5] Update `README_DEV.md` agents tree** (context-engineer) — add `architect-validator.md` and `interface-designer.md` to the agents directory listing.

8. **[T02 monitoring] Watch always-loaded token trend** (user) — headroom dropped from 10.3% to 6.3% in 4 days. No immediate action required, but the next unconditional rule addition or CLAUDE.md expansion should be preceded by a `wc -c` budget check.

9. **[TD13 optional] Consider retiring td-013 as `wontfix`** (user) — `metrics.py` at 503 lines with stable churn is below the 800-line ceiling and no longer in the top-10 hotspot list. If the team accepts the current complexity density, this row can be closed.
