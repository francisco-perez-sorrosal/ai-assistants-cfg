# SCENARIO: code-file (allowlisted) OK target referenced from alpha/SKILL.md
# Exists solely so the OK code-file link resolves.


def _placeholder() -> None:
    """No-op placeholder for the fixture corpus."""
    return None
