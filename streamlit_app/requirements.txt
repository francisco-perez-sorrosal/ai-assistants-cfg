# Praxion Pipeline Dashboard — dependency manifest
#
# Install model: single user-mode install into ~/.praxion-dashboard/venv/
# (created by `scripts/praxion-dashboard install`).
#
# This file is NEVER installed into the user's project venv or Praxion's
# repo-level pyproject.toml.  It exists solely to populate the dashboard venv.
#
# Version rationale
# -----------------
# streamlit     1.55.x  — first version with st.Page / st.navigation (multi-page
#                         navigation API) and st.fragment(run_every=) for
#                         Workshops page auto-refresh.
# plotly        5.*     — heatmaps, bipartite, line charts; already a transitive
#                         dependency of streamlit so effectively zero extra cost.
# pyyaml        6.*     — ADR + artifact frontmatter parsing (stdlib re + pyyaml
#                         per dec-draft-1c4350fd); transitive of many Streamlit
#                         deps as well.
# pandas        2.*     — METRICS_LOG.md and SENTINEL_LOG.md parsing; transitive
#                         of plotly so already present.
# pytest        8.*     — data-layer behavioral tests (Steps 3b–6b, 7b–12b).
#
# Intentionally excluded
# ----------------------
# streamlit-mermaid-interactive — Mermaid deferred to v2 (dec-draft-d57dc712).
# streamlit-autorefresh         — banned; use st.fragment(run_every=) instead.
# pyvis                         — deferred until Step 8a (ADRs interactive DAG)
#                                 confirms the need; do not pre-install.

streamlit>=1.55,<1.60
plotly>=5,<6
pyyaml>=6,<7
pandas>=2,<3
pytest>=8,<9
