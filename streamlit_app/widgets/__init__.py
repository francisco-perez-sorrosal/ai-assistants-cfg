"""
Shared widget modules for the Praxion Pipeline Dashboard.

Widgets are Streamlit UI components reused across multiple pages.

Sub-modules
-----------
  artifact_card  — card with title, body, and "What is this?" popover
  educational    — educational popover linking to producer skill/rule/agent
  graph          — visualization helpers (ADR supersession DOT, Plotly sparkline)
  empty_state    — graceful degradation widget when an artifact is absent
"""
